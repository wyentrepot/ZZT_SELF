name: observe-workbench-logs
description: >-
  HPLC meter-reading workbench AI observation skill (read-only). Query status,
  create bounded observations on existing module sessions or listener indexes,
  wait to terminal state, read artifacts, and fetch listener frame detail.
  Observation-only: never ensures/starts/stops/flashes serial or scans files.
metadata:
  author: reasonix
  version: "1.0.0"
  applies-to: D:/2-侦听台改造
policy:
  allow_implicit_invocation: false
