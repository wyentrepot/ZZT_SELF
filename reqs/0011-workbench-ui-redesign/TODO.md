# TODO-0011：工作台 UI 重设计 Demo

> 基线：`ui-demo/功能清单.md`（9 页 + 外壳 / 94 项）
> 流程铁律：**先清单 → 再设计 → 后核对**。禁止凭印象增删功能。
> 产物：`ui-demo/workbench-ui-demo.html`（单文件）

## P0（已完成 2026-08-30）：功能基线

- [x] T001 通读 `apps/workbench/static/` 全部前端文件（含 `pages/*` 九个子页面）
- [x] T002 产出 `ui-demo/功能清单.md`：9 页 + 外壳共 **94 项**，按 验证/设备/辅助/维护 分组
- [x] T003 确认 `apps/listener/`、`apps/module_log/` 独立服务版与 workbench 集成版**功能同源**
      （md5 + diff 比对，差异仅接口前缀 `/api/logs/*` vs `/api/listener/logs/*`），不重复计入
- [x] T004 登记 v1 的 16 处功能缺失（含用户点名的 3 项），写入清单末尾「上一版 Demo 的功能缺失」

## P1（进行中）：Demo v2 重建

### 结构与样式

- [x] T101 主题 Token 补齐：4 套主题 CSS（`theme-deepblue/emerald/charcoal/indigo`），
      配色取自生产 `apps/workbench/static/tokens.css`；`inconclusive` 独立紫色相 `#A371F7`
- [x] T102 外壳 HTML 结构（0.1-0.9）：顶栏品牌区 / 4 主题圆点 / 侧栏折叠 / 状态栏 /
      分组导航容器 / iframe 保活说明 / hash 路由 / 主题广播 / 窄屏抽屉（<860px）
- [x] T103 验证工作台 HTML（1.1-1.9）：场景+固件版本/commit/日志目录、跳过烧录/激励/反馈、
      执行/取消 Run、Run 结果、证据下钻、历史 Runs
- [x] T104 串口配置 HTML（2.1-2.6）：四槽 Profile、保存/一键应用/刷新状态、错误汇总条、
      槽启用开关 + 字段表单 + 槽状态 + 结果反馈
- [x] T105 模块日志 HTML（3.1-3.21）：3 页签 = 实时日志（动态会话页 + 串口/烧录参数 +
      日志框 + **串口发送框 ← 用户点名补回**）/ 对照解析（来源二选一 + 行号漂移 + 事件↔日志双向绑定）/
      模拟集中器内嵌版；文件选择对话框
- [x] T106 侦听台 HTML（4.1-4.29）：**全局 NID 筛选条 ← 用户点名补回** + 5 视图 =
      新版帧浏览（三列）/ 帧浏览经典（01 数据源 + 02 帧索引 + 03 深度解析）/
      **分钟采集分析 ← 用户点名补回** / 任务配置统计 / 网络承载评估（canvas 趋势图）
- [x] T107 模拟集中器 HTML（5.1-5.7）：串口控制（含 Profile anhui/test）+ AFN / Fn / 参数与下发
      三列 + 下发区 + 内置应答条 + 可折叠收发记录
- [x] T108 报文追踪 HTML（6.1-6.3）：追踪任务（回放/Live 分段 + 新建 + 刷新 + 计数）/
      特征编辑 / 三段证据链
- [x] T109 协议字典 HTML（7.1-7.3）：字典 / 条目 / 详情 三列 + 搜索 + 计数
- [x] T110 场景脚本 HTML（8.1-8.5）：场景 / 步骤编排 / 步骤详情 三列 + expected_flow 时间线
      + 「去验证工作台执行」跳转
- [x] T111 工作台状态 HTML（9.1-9.2）：平台信息（应用名/版本/两个挂载✓）+ 主题说明

### 交互逻辑

- [x] T112 注入完整交互 JS（替换 `/*SCRIPT*/` 占位，约 104 KB）：
  - [x] T112.1 工具层（确定性伪随机 LCG seed=20260830、DOM 助手、toast、文件对话框）
  - [x] T112.2 外壳：导航渲染（4 组 9 页）+ hash 路由 + 侧栏折叠 + 密度切换 + 主题切换与广播
  - [x] T112.3 验证工作台：场景加载 / Run 800ms 轮询至终态 / 取消 Run / 证据下钻 / 历史刷新
  - [x] T112.4 串口配置：四槽渲染 / 保存校验 + 错误汇总条 / 一键应用 / 刷新状态
  - [x] T112.5 模块日志：会话页增删 / 串口启动 / 烧录进度 / 日志流 / **串口发送框**
        / 对照解析双向绑定 / 内嵌 simcon 规则与任务
  - [x] T112.6 侦听台：全局筛选 + chips / 5 视图切换 / 帧索引分页 / 深度解析页签
        / 分钟采集 / 任务配置 / 网络评估 canvas
  - [x] T112.7 模拟集中器：AFN→Fn 联动 / 语义化参数 / 构帧预览 / 下发 / 收发记录分段
  - [x] T112.8 报文追踪 / 协议字典 / 场景脚本 / 工作台状态 四页渲染

### 校验与交付

- [x] T113 静态校验（`ui-demo/_check.js`）：JS 语法 PASS / 258 个 id 引用零悬空 /
      9 导航 ↔ 9 section 全映射 / 标签开闭平衡 / 4 套主题 CSS 齐备
      - 注：首轮报 `<tr> 28 开 / 29 闭` 为**校验脚本误报**（JS 里 `'<tr' + x + '>'` 拼接写法未被正则计入），
        已修正正则为 `<tag(?=[\s>'"`])` 后复验通过
- [x] T113b **运行时冒烟**（jsdom，`ui-demo/_smoke.js`）：**112 条断言全通过 / 0 失败 / 0 运行时错误**，
      9 页全覆盖（外壳 13 / 验证工作台 9 / 串口配置 7 / 模块日志 21 / 侦听台 32 / 模拟集中器 12 /
      报文追踪 4 / 协议字典 5 / 场景脚本 5 / 状态与主题 8）
      - 修复 1 处**真实缺陷**：串口配置 `change` 后整体 `renderSlots()` 重绘 → 丢焦点、
        清空槽结果、旧节点 dispatch 不再冒泡。改为新增 `updateSlotCard(i)` 局部刷新状态行
      - 修复 2 处**冒烟脚本自身**问题：`$$('#detailBase').parentNode`（`$$` 返回数组）→
        改用 `.tab[data-dt="app"]`；对照解析「点日志行」改为点事件所在行（`data-ln`）
- [x] T114 **94 项功能逐条核对表**（`ui-demo/功能核对表.md`）：**94 / 94 覆盖（✅ 93 · ⚠️ 1）**，
      每项给出 Demo 具体落点（元素 id / 区块）；含「用户点名三处专项复核」与「v1→v2 缺失补齐对照」16 项
      - ⚠️ 仅 0.6：单文件 Demo 无 iframe，iframe 保活改为设计说明抽屉内**明文说明**该机制
- [x] T115 修复校验发现的问题并复验（共 6 项，详见核对表末节）：
      - 串口配置整体重绘 → `updateSlotCard(i)` 局部刷新（真实缺陷）
      - 补分钟采集「错误横幅」`#minuteErr` + `minuteQuery()` 入参校验（清单 4.16 功能缺失）
      - `parseInt(v,10) || 15` 把周期 `0` 吞成 `15`，非法值绕过校验 → 改 `isNaN(pv) ? 15 : pv`（真实缺陷）
      - 冒烟脚本 `$$(...).parentNode` / 对照解析断言写法（脚本 bug）／静态校验正则误报（校验脚本 bug）
      - 复验：静态全绿；冒烟 **114 断言通过 / 0 失败 / 0 运行时错误**

## P2（待启动）：交付

- [x] T201 `ui-demo/overview.md` 更新为 v2 说明：含三处点名补回落点、v1→v2 补齐 16 项、
      4 套主题与五态色相设计、自动校验结果、发现的生产问题（tokens.css inconclusive）、
      文件清单与下一步待拍板项
- [x] T202 呈现给用户（present_files：Demo HTML + overview.md + 功能核对表.md）

## P3（⏸ 挂起）：生产改造

- [ ] T301 Demo 通过后，评估生产 UI 改造范围 —— 并入 REQS-0010 P5「壳层主题统一」决策，不单独拍板
