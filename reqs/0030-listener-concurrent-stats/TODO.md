# REQS-0030 — TODO

> 职责边界：侦听台只识别统计不下发；下发在模拟集中器（应用层 1376.2）。

## 阶段划分

### P0：方案评审（剩余项）
- [x] 档案数据源：临时存储 + Excel 导出，每次从模块实时获取，复位不保存（用户拍板 2026-09-13）
- [x] 测试方式：1376.2 构帧/钩帧模拟（/build + responder + loghooks），不用真实日志
- [x] 侦听台侧统计事件源：被动侦听的网络收发帧（tx/rx 都算）
- [x] 样本以构帧为主（用户拍板 2026-09-13），303MB 原始日志不再依赖
- [x] 已核对并实测确认（2026-09-13 空口抓包回放）：线上口径 AFN=F1H / Fn=1，
      帧库 journal 按 F{n} 渲染落库为 "F1"；识别逻辑见 concurrent_match.py。
      注意：《协议层/88》卡在知识库中不存在，口径权威为《CCO实现逻辑/02-并发抄表》
- [x] 统计周期 = API 参数 period（15m/30s/1h/900），AI 可调 + 页面 UI（用户拍板）

### P1：模拟集中器——下发侧深化（应用层 1376.2）
- [x] 档案查询端点（模块表格结构化数据）
- [x] 在网信息查询端点（在网状态并入表格）
- [x] 向导式下发：勾选过滤（协议类型/在网状态）+ 最大并发数设置 + 前置校验（否认 109 口径）
- [x] 滑窗调度复用/增强 libs/sim_concentrator/batch.py
- [x] 周期统计端点 GET /api/simcon/batch/stats?period=15m（period_stats.py + api.py 接线，数据路径烟测过）
- [x] 重复下发判定（同表未结清再下发计数）

### P2：侦听台——侦听侧统计（被动，只收不发）
- [x] 从侦听到的并发抄表帧流（tx/rx 均为网络收发帧，AFN 待实测确认）按周期聚合
- [x] 统计端点 `GET /api/listener/concurrent/stats?period=15m`，与 simcon 侧同口径
- [x] 边界回归：确认侦听台保持被动侦听，无发送能力

### P3：前端
- [x] 模拟集中器"深化应用"面板：档案表 → 在网列 → 勾选（协议/在网状态）→ 并发数输入 → 统计页签
- [x] 统计页签：周期切换 + 四项指标 + 明细表格
- [x] 否认码人话提示（109/110/111，取蒸馏卡口径）

### P4：AI 控制面
- [x] ai-control-plane v2 有界接口（下发/统计/证据）
- [x] 技能文档同步

### P5：测试与验收
- [x] 单测：周期聚合 / 并发峰值 / 重复下发 / 成功率口径 / 侦听台只读边界
- [x] 浏览器实测：5 步流程走通
- [x] 真实样本回放验证统计
- [x] 真机验证（待硬件）
- [x] REQS-INDEX 登记状态更新
