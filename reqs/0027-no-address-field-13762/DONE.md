# REQS-0027 — DONE

> 状态：进行中（待 P1/P2 完成后再归档）
> 创建：2026-09-02

## 已确认

1. **问题复现**：`anhui` profile 含 `cco_addr=020103040506`，`build_address` 装配带地址域
   下行帧（`module_id=1`），集中器回否认 `AFN=00 Fn=F2 错误状态 0A（主节点不支持此命令）`。
2. **正确帧**：无地址域 10H-F2 查询帧
   `68 12 00 43 00 00 00 00 00 04 10 02 00 00 00 10 69 16`
   （信息域 `module_id=0`、无地址域 A、校验 CS=69 通过）。
3. **实机验证通过（2026-09-02）**：以 `format: local` 语义化下发无地址域 10H-F2
   （`68 13 00 43 00 00 00 00 00 05 10 02 00 00 00 00 10 6A 16`），集中器正常应答
   `68 12 00 83 00 00 40 00 00 05 10 02 00 78 00 00 52 16`（AFN=10 Fn=F2，从节点总数量 **120**），
   不再返回 `0A` 否认。证明"1376.2 下发必须不带地址域"。
4. **实机清空档案（2026-09-02）**：查询到 120 个从节点档案，分 8 批（每批 ≤16）
   下发 11H-F2 删除，复查 total=0 确认清空 ✅。
   - **期间发现并修复**：`build_address` 误把 `params.meters`（11H-F2 删除对象清单）
     视为 1376.2 路由地址域目标而装配地址域，导致首轮删除帧带地址域（可能被集中器忽略）。
     已从 `has_explicit_target` 与 dst 解析中移除 `meters`，删除帧恢复无地址域
     `68 70 00 43 00 00 00 00 00 09 11 02 00 10 ...`，复查成功。
