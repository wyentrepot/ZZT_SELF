# REQS-0027 TODO

> v2（变更 2）：新增 G4 统一抄读表格+成功率、G5 并发滑窗调度、G6 上报分类型表格；阶段相应扩为 5 个。

## 阶段 1：应答预期规则库 + 超时档位（后端）
- [ ] `libs/sim_concentrator/expect_rules.json`：覆盖常用 AFN（00/02/03/05/06/10/11/F1）的应答形态映射 + 否认码 6D/6E/6F 语义 + 超时档位（默认 5s；单抄 59s；并抄 99s；门限标注蒸馏出处）
- [ ] matcher.py：默认 expect 自动生成（显式 expect 可覆盖）+ reasons 中文化 + 否认码识别
- [ ] runner.py：per-Fn expect_timeout；匹配窗口内持续等待（对齐 expect_history 轮询语义）
- [ ] api.py：/step 响应增加配对结构（下发摘要/应答形态/超时档位/旁听帧列表）；新增 GET /expect_rules
- [ ] 单测：expect_rules 加载与静态校验、默认 expect 生成、否认码判定、超时档位取值

## 阶段 2：并发抄表滑窗调度器（后端，G5 核心）
- [ ] runner.py 新增并发任务模式：表队列 + 最大并发数（可配，默认档位口径参考 CCO aps_max_trans/蒸馏事务框架）+ 滑窗调度（应答成功/失败/超时均释放槽位，立即补发下一块）
- [ ] 每块表独立套 per-Fn 超时档位与重发窗口；超时释放槽位计入失败
- [ ] api.py 并发任务会话端点：创建任务（表清单+最大并发）/查询进度（在途/完成/成功/失败/排队）/获取明细
- [ ] 单测：滑窗保持最大并发、槽位释放三路径（成功/否认/超时）、队列耗尽收敛、6D 超并发拒绝路径

## 阶段 3：抄读汇总与上报计数聚合（后端，G4/G6 数据面）
- [ ] 抄读数据汇总结构：单抄/并抄/分钟采集共用（表地址/AFN-Fn/数据项/值/时间/耗时/结果），失败按否认码细分；统计行（下发/应答/成功/失败/成功率）
- [ ] 主动上报计数聚合：AFN=06H 按类型分桶（F1 从节点信息/F2 抄读数据/F3 路由工况/F4 信息+设备类型/F5 事件——停复电子类 01H/02H 单列）
- [ ] api.py 汇总查询端点（可按方案/时间段/结果过滤）；上报历史端点扩展分类型+计数
- [ ] 单测：成功率口径（超时算失败）、分桶计数、停复电事件解析

## 阶段 4：前端（双副本，G1-G6 全量）
- [ ] 下发区改造：下发后呈现"下发 ↔ 应答"配对卡片（下发摘要/hex ↔ 应答）；分格式渲染（查询类→数据表格；控制类→确认/否认徽标+人话；设置类→确认+参数回显；嵌套 645/698 沿现有解码）
- [ ] 等待倒计时/进度 + 超时显式标注（含门限出处 tooltip）；旁听帧单独归类展示
- [ ] 统一抄读数据表格：顶部统计行（含**成功率**）+ 筛选（方案/时间段/结果）+ 实时追加
- [ ] 并发任务面板：最大并发数输入 + 在途/完成/排队计数条 + 表格实时滚动
- [ ] 主动上报分类型表格：每类一表（停复电单独一表）+ 数量统计头；页签或分组切换
- [ ] 双副本同步：apps/listener/static/ 独立版（API 前缀参数化）

## 阶段 5：验收与收尾
- [ ] 浏览器实测：查主节点地址（确认）、查从节点信息（表格）、设置类（确认徽标）、单抄 59s/并抄 99s 档位不假超时、否认路径（构造超并发 6D）
- [ ] 并发滑窗实测：多表队列 + 最大并发=N，观察"回一帧补一发"、在途计数条、成功率统计正确
- [ ] 上报实测：停复电事件分表 + 计数（真机或 replay 样本）
- [ ] simcon 既有回归全绿（test_runner/test_responder_matcher/test_api_cli 等）
- [ ] tools 页双副本同步核对（防浏览器缓存假"未生效"）
- [ ] REQS-INDEX/TODO/DONE 状态同步；提交 req/0027 分支
