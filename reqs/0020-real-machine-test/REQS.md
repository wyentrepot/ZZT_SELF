# REQS-0020 — 实机运行测试（集中器分钟采集方案 + 645 任务5 下发）

> 状态：🚧 进行中
> 创建：2026-09-02
> 分支：`req/0020-real-machine-test`
> 关联：REQS-0018（simcon 只读查询）、REQS-0019（解析服务）、ai-control-plane 技能

## 1. 背景与目标

已完成串口映射（COM24=侦听台 / COM4=集中器 / COM9=CCO / COM8=STA）与 Windows 解析服务。
本次为**实机运行测试**，目的是**记录使用的困难点，找出项目优化点**。

## 2. 本次执行场景

1. 启动服务（workbench 8790 + 侦听台 8765 + 模块日志 8766），后台常驻监听。
2. 集中器（simcon，COM4）下发分钟采集方案。
3. 先执行**档案读取**。
4. 对 1-32 号档案下发 **645 协议 5 号任务**：周期 10min，采集项 A 相电压。
5. 下发后服务保持后台运行一直监听数据。

## 3. 边界 / 停止条件

- 本次为实机验证，不改固件、不烧录。
- 硬停止条件：**15 分钟运行时间**，到时无论完成与否都停止本轮任务。
- 服务保持后台常驻监听；AI 任务本身可在下发完成后结束。

## 4. 产出

- 实机交互记录（simcon 帧日志 / 侦听台索引 / 模块日志观察）。
- 困难点清单 → 项目优化点建议。
