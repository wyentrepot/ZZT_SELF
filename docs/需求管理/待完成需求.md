# 待完成需求

> **文档用途**：汇总本项目所有未完成/待确认/依赖外部输入的需求与遗留项，作为需求现状的「待完成」侧唯一入口。
>
> **维护方式**：完成一项即移入 `docs/需求管理/已完成需求.md` 并标注完成日期；新增遗留项追加到末尾。
> **归档底稿**：`docs/需求管理/归档/代办事务.md`、`docs/需求管理/归档/oad_todo.md`、`docs/需求管理/归档/任务交接需求与进度表.md`、`docs/需求管理/归档/分钟采集帧结构_待确认知识点.md`、`docs/需求管理/归档/分钟采集问题清单_待AI确认.md`。

---

## P0 — 高优先级

### T-001：一键打包发布改造

- **状态**：📋 待实施（P0，预估 1-2 小时）
- **来源**：`docs/需求管理/归档/代办事务.md`（T-001）；方案：`docs/需求设计方案/一键打包发布方案.md`
- **需求描述**：新建根目录 `一键打包-发布.bat`，一键打包到 `发布/` 文件夹。产物拷贝到其他 Windows 10/11 电脑即可直接使用，无需安装 Python 或依赖。**发布目录取代现有 `dist/` 目录**。
- **验收标准**：
  1. 双击 `一键打包-发布.bat`，自动完成安装 PyInstaller → 打包 → 冒烟测试全流程，退出码 0；
  2. 产物在 `发布/侦听台/` 目录下，包含 `侦听台.exe` 和 `_internal/` 子目录；
  3. 冒烟测试通过：`/api/version` 返回 `GW_SMAnalysis`，首页可访问，索引+分页正常，`runtime/` 落在 exe 同目录；
  4. 拷贝 `发布/侦听台/` 到其他 Windows 10/11 电脑，双击 `侦听台.exe` 可正常启动服务并打开浏览器；
  5. `dist/` 已移除，`发布/` 的 Git LFS 跟踪和 .gitignore 忽略规则正确生效。
- **涉及改动**：`一键打包-发布.bat`（新建）、`.gitattributes`（LFS `dist/**`→`发布/**`）、`.gitignore`（`dist/`→`发布/`）、`tools/scripts/smoke_test_packaged.py`（exe 路径）、`tools/packaging/hplc_parser.spec`（注释）、`tools/packaging/build_exe.bat`（标注废弃）、`dist/`（`git rm -r`）。

### R-06 遗留：并发抄表帧数据单元循环 / 请求-响应关联

- **状态**：🔶 主体已完成，字段级展开待协议专项
- **来源**：`docs/需求管理/归档/任务交接需求与进度表.md` §6.1/§6.4
- **当前进展**：DLL 已识别 `0001/0002/0003` 抄表帧，Python 已按规约类型递归解出全部内嵌 698/645 帧，真实大日志验收 200/200。
- **遗留点**：
  - 数据单元循环结构与请求/响应序列关联的完整展开，仍依赖后续协议专项；
  - DLL 简单摘要中电能表地址取 6 字节后，需真实验收确认显示正确。

### FR-5 / FR-6：AI 闭环编排层 + 统一集成程序（platform）

- **状态**：📋 待开发（P0，产品形态）
- **来源**：`docs/总设计框架/AI闭环平台项目设计需求文档.md`（FR-5/FR-6、§7 差距矩阵）；详细设计 `docs/需求设计方案/platform-一体化工作台详细设计.md`（评审稿）
- **差距矩阵**（归档 §7）：

| 需求 | 现状 | 差距 | 优先级 |
|---|---|---|---|
| FR-5.1 Run 抽象 | ❌ 无 | 新增 run 管理 | P0 |
| FR-5.2 统一报告 | ❌ 无 | 新增 report schema 与聚合服务 | P0 |
| FR-5.3 期望流程比对 | ⚠️ sequence 有雏形 | 升级为通用流程投影 + 前端渲染 | P0 |
| FR-5.4 归因反馈 | ❌ 无 | 新增归因规则 + 反馈组装 | P1 |
| FR-6 统一集成程序 | ❌ 无 | 新增 `platform/` 包 + 统一前端 + 编排路由 | P0（产品形态） |

- **范围**：把侦听台、模块日志/烧录、对照解析、模拟集中器与 AI 验证工作台集成为一个程序（页签式）；**只挂载 + 编排 + 统一外壳，不合并底层代码**。
- **详细设计待评审问题**：见 `platform-一体化工作台详细设计.md` §10。

## P1 — 中优先级

### FR-1 差距：sequence 可选步 / 循环 / 负向断言

- **状态**：📋 待开发
- **来源**：PRD §7 / §8.1 演进方向
- **内容**：sequence 步骤增加 `optional`（可选步）、`repeat_until`（循环重试）、`negate`（负向断言）；支持 `timing`（如 step2 必须在 step1 后 5s 内）。

### FR-2 差距：场景库模板化 + golden 回放

- **状态**：📋 待开发
- **来源**：PRD §7
- **内容**：模拟集中器场景库模板化；协议一致性用真实抓包 golden data 回放。

### OAD/OI 覆盖扩充

- **状态**：📋 待扩充（P1）
- **来源**：`docs/需求管理/归档/oad_todo.md`
- **现状**：698.45 标准附录 A OI 总数 515，当前 `oad.json` 仅 18 个，覆盖率 3.5%。
- **按类别缺失**：
  - 事件类：缺失 99 个（3002 过压、3003 断相、3004 失流 … 3306 电控跳闸记录单元等，清单见归档原文）；
  - 变量类：电压/电流/功率/频率等所有分相 OAD（中优先级）；
  - 参变量类：通信参数、波特率、协议版本等（低优先级）；
  - 控制类、文件传输类、ESAM 接口类等（低优先级）；
  - 645 协议 DI：按实际抄表业务遇到的未知 DI 逐步补充。
- **生成工具**：`tools/scripts/analyze_oad_coverage.py`（输出路径已同步至 `docs/需求管理/归档/oad_todo.md`）。

### 缺失 STA 分析（依赖外部输入）

- **状态**：📋 阻塞（需外部基准）
- **来源**：`docs/需求管理/归档/任务交接需求与进度表.md` §12.5
- **内容**：需要未来的**期望站点清单**（expected-station roster）作为基准，才能做缺站判断；不凭空声明某 STA 缺失。

### 真实大日志验收（按用户指定区间）

- **状态**：📋 待安排
- **来源**：`docs/需求管理/归档/任务交接需求与进度表.md` §4 阶段 8 / §6.3
- **内容**：大文件验收只做受控抽样或用户指定时间/帧序号区间，不反复扫描原始大文件。

## P2 — 待确认 / 协议专项

### 分钟采集帧结构待确认（Q1~Qn）

- **状态**：📋 待协议确认
- **来源**：`docs/需求管理/归档/分钟采集帧结构_待确认知识点.md`
- **要点**：
  - Q1：0x00E3 字节 6 位分配（协议类型 3bit / 电表类型 2bit / 保留 3bit）是否正确？
  - Q2：0x00E3 冻结时刻是否小端 BCD？
  - Q3：0x00E2 字节 1 bit4~7 是否全保留、无方向位？
  - Q4：0x00E2 上行应答帧结构是否正确？
  - Q5：0x00E4 格式一（并发抄读格式）23 字节报文头结构是否正确？
  - （完整问题清单见归档原文）

### 分钟采集解析代码待确认问题（7 项）

- **状态**：📋 待 AI/协议确认后修正
- **来源**：`docs/需求管理/归档/分钟采集问题清单_待AI确认.md`
- **要点**：
  1. 0x00E3 协议类型位宽用错（`& 0x0F` 应为 3bit）；
  2. 0x00E3 电表类型位宽/偏移错（`(b>>4)&0x01` 应为 `(b>>3)&0x03`）；
  3. 0x00E3 冻结时刻未按小端 BCD 解码；
  4. 0x00E2 上行应答帧未解析（缺失 `_parse_minute_config` 应答方法）；
  5. 0x00E2 下行解析多一个「方向」字段（文档无方向位定义，不影响功能）；
  6. 0x00E4 格式一（并发抄读格式）未展开字段解析；
  7. adapter_10376 报文 ID 字典缺 0x00E2/E3/E4（显示为「未知报文 ID」）。

### 前导字段含义待确认

- **状态**：📋 待协议文档确认
- **来源**：`docs/需求管理/归档/任务交接需求与进度表.md` §12.1
- **内容**：真实样本源 MAC 之前存在 1 个未文档化字节（如 `01`），当前按「前导字段」原样展示，命名与含义待确认后修正。

## P3 — 历史遗留 / 可选

### 反编译旧工具 GwMixAnalysis 解析逻辑

- **状态**：📋 未完成（工具受限）
- **来源**：`docs/需求管理/归档/任务交接需求与进度表.md` §11
- **现状**：旧工具 `GwMixListener.exe` 内含 100 个 `GwMixAnalysis` 解析模块 `.cs`（PDB 可还原工程路径，但本机无源码）。
- **障碍**：`ilspycmd` 经 `dotnet tool` 安装失败（NuGet 包缺 `DotnetToolSettings.xml`）；GitHub ILSpy x64 包下载超时。
- **建议**：优先反编译 `GwMixAnalysis` 100 个文件提炼 MPDU/MAC/APS 解析逻辑（估算 40-80 万 token）；可用 `ildasm.exe`（NETFX 4.8 Tools）先导 IL。

### 已知测试基线问题

- **状态**：📋 需在 Windows + DLL 环境终验
- **来源**：`docs/需求管理/归档/任务交接需求与进度表.md` §15 / §20.4
- **内容**：
  - `test_concurrent_meter_e2e.py::test_all_frames_enrich_nested_698` 曾有失败记录（涉及并发抄表解析，非本轮改动引起）；
  - `test_list_real_workspace_directory_finds_sample_log` 依赖本机未入库的「测试文件」数据目录（与改动无关）。
