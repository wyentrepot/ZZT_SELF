# 17-P8 独立真机验收清单

> 文档性质：**只列清单、不实际执行**的独立真机验收项（P8 交付门最后一环）。
> 生成日期：2026-08-24
> 依据：`docs/_archive/2026-08-24/P3-P6交接清单.md` §3 P8、P1-P6 实现（ADR-30~35）。
> 前置：真实串口环境（COM4 侦听 / COM9 CCO 日志 / COM8 STA 日志 / COM24 模拟集中器，见 `docs/15-硬件资源速查.md`）、物理映射模板 `config/serial_ports.json`、已打包 `dist/工作台/工作台.exe`。
>
> ⚠️ 以下所有步骤**不得在本清单外自动执行**；真实串口启动、发送、烧录必须在当次操作由用户**单独明确授权**后进行。

## 0. 验收执行记录（2026-08-24 真机）

- **执行环境**：WSL（Ubuntu-22.04）源码运行 workbench（`python -m workbench.run`，端口 8790），未打包。用户接受「源码态 `data/runtime/` 等效观测」替代打包态 `exe 同级 runtime/`。
- **串口映射（真机核对，reboot + fcversion/bootloader）**：COM4=/dev/ttyUSB0 侦听台、COM9=/dev/ttyACM1 **CCO**（venus8m，fcversion hversion=0201）、COM8=/dev/ttyACM0 **STA**（venus2m_v7，fcversion hversion=0101）、COM24=/dev/ttyUSB1 模拟集中器。原 `serial_ports.json` 将 CCO/STA 映射反了，**已对调修复（commit bc08e26）**。
- **真实缺陷修复**：① `resolve_bin_path` 平台感知（Linux 不再转 UNC，commit bc08e26）；② P4 applier 平台相关测试（commit 5bb348b）；③ 烧录 baud_plan 导航波特率实际为 115200（非固件名暗示 9600），用 `[115200]` 烧录成功。
- **真机操作授权**：四槽 apply（含侦听台）、fcversion/reboot/version 命令、iap_cco 烧录——均经用户单独明确授权。
- **未验证项及原因**：
  - §1 第 3 条「打包产物 exe」：用户接受源码态等效观测，未打包。
  - §3 前端浏览器交互（导航/抽屉/hash）：WSL 无 GUI 浏览器，由自动化测试 `test_shell_navigation.py`（8 passed）覆盖 JS 逻辑；后端会话保持已真机验证。
  - §6 第 3 条「烧录后 CCO/STA 交互（698/分钟采集）」：本次烧录后未执行完整 698 采集交互（属既有 task 验证范畴，待后续真机窗口）。

## 1. 前置与环境核对

- [x] 真机环境可用：4 个串口（COM4/COM8/COM9/COM24）全部枚举可见（`/api/module-serial/ports` 的 `port_details` 一致，4 个 USB 串口 `online: true`）。
- [x] `serial_ports.json` 物理映射与真机一致；`mapping_error` 为空（`""`）。
- [x] **源码态等效**：workbench 启动后 `data/runtime/` 已生成（PUT 保存后 `serial_profile.json` 持久化）；`serial_ports.json` 从仓库 `config/` 读取。
- [x] 默认 Profile 四槽均为 `enabled:false`，启动不自动打开任何串口（GET /api/serial-profile 首次返回全 false，未落盘）。

## 2. 四槽 Save / Apply（P3+P4 串口 Profile）

- [x] **保存（PUT /api/serial-profile）**：四槽分别配置启用/映射/波特率并保存；刷新后持久化（原子写入 `data/runtime/serial_profile.json`）。
- [x] **一键应用（POST /api/serial-profile/apply）**：应用后按固定顺序（侦听台→CCO→STA→模拟集中器）逐槽状态符合预期（`started/reused/stopped/unchanged/skipped/failed`）。
- [x] **刷新状态（GET）**：应用后各槽 `status`/`占用`/`应用结果` 反映真实串口状态。
- [x] 重复应用相同 Profile：已运行槽返回 `unchanged/reused`，不重复打开串口。
- [x] 禁用槽应用：仅停止 `托管-` 前缀会话，**不影响**人工动态会话。
- [x] `serial_ports.json` 只读：应用后该文件不被修改（Profile 独立存储，md5/mtime 不变）。

## 3. 页面保活（P5 导航 + iframe）

- [x] 一级分组导航（验证/设备/维护）切换：已加载 iframe 不重设 `src`，不重载页面（自动化测试 `test_shell_keeps_lazy_iframes...` 覆盖 JS 逻辑）。
- [x] hash 路由：切页后 `location.hash` 正确；刷新/前进后退恢复到对应页（自动化测试 `test_hash_*` 覆盖）。
- [x] 折叠/抽屉：桌面端侧栏折叠为图标栏；窄屏抽屉可键盘关闭（Escape/遮罩/焦点回归）（自动化测试 `test_drawer_*`/`test_collapse_*` 覆盖）。
- [x] **运行会话保持**：串口会话运行中切页/刷新后，会话不被 stop（真机验证：多次 apply/GET 后 cco/sta/simcon 会话持续 running；前端 iframe 保活逻辑由测试覆盖）。

## 4. 部分失败（P4 applier + P6 配置页）

- [x] 人为制造单槽失败（串口被占用）：单槽 `failed`，后续槽继续，不回滚成功项（真机：sta 槽指向被 cco 占用端口 → failed，其余 unchanged/reused，overall=partial）。
- [x] 配置页部分失败：保留成功槽状态，错误摘要可聚焦（`tabindex`+focus）（自动化测试 `test_focusable_error_summary` 覆盖）。
- [x] 重复映射：两个槽选同一映射时 apply 前阻止，零副作用（前端 `collectProfiles` 校验，测试覆盖）。

## 5. 实时 observation 起始边界（P1+P2 AI 观察）

- [x] module live `observe`：从 `start=now` 监听，`regex`（`rf sof` matched）、`sequence`（rf sof→REMAP_ING matched）、`loghook_rule`（`common.join_onnet` timed_out 不误报）、`not_seen`（超时 live_window_unverified）各验证一次真实日志。
- [x] `not_seen` 逾期且无可信行到达时间：返回 `timed_out + condition_met=false + reason=live_window_unverified`（不误报成功）。
- [x] listener `cursor_range`：按 `index_id + frame_id` 深链观察（frame 1~100 matched，深链 detail_url/ui_url）；越界（frame 100000）返回 422「超出索引边界」；`wait` 单次 ≤30s、终态即停。
- [x] `observe-workbench-logs` 技能：默认 dry-run 零 HTTP（status/observe/frame-detail 均输出 execute:false 计划）；`--execute` 对不存在 session 返回 404 安全失败、对既有 session 成功创建 operation。
- [x] Token：`WORKBENCH_AI_TOKEN` 生效；401 时输出 `Bearer <redacted>` 脱敏不泄漏；六命令只读无控制操作命令（测试 19 passed）。

## 6. 烧录后因果验证（P2 观察 + 真机）

- [x] 烧录完成后：module 日志能观察到 bootloader → 应用启动关键日志（烧录日志：XMODEM 454/454 包 → `Xmodem download 464896(bytes) success!` → `BURN SUCCESS` → `Unicorn Bootloader` → `Run image0` → `boot_ver/sdk_ver` → 应用日志）。
- [x] 烧录前后状态可区分：`observe` 在烧录前后窗口能取到因果证据（烧录后 regex observation matched，log 指向烧录后会话日志，生成 artifact）。
- [ ] CCO/STA 烧录后交互（698/分钟采集）行为与预期一致（结合既有 task 验证）——**未执行**：本次烧录后未运行完整 698 采集交互，待后续真机窗口（属既有 task 验证范畴）。

## 7. 通过标准

- [x] 2~6 全部项通过（或每项如实记录未验证原因）：§2/§4/§5/§6(1,2) 真机通过；§3 自动化测试+后端会话保持验证；§6(3) 记录未验证原因。
- [x] 结果回填：更新 `docs/17-P8真机验收清单.md` 勾选状态 + `docs/04-任务安排.md` P1-P8 状态表 + 交接报告。
- [ ] 真机验证项全部完成后，P8 才算正式交付门关闭；P7（目录整理）仍为独立后续项（§6(3) 未完成，P8 待最终关闭）。
