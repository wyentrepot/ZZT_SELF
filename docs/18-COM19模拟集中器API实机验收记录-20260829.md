# COM19 模拟集中器 API 实机验收记录（2026-08-29）

> 结论：**API/串口映射/会话帧日志通过；CCO 业务交互未通过或未验收。**
>
> 范围：验证已提交的需求 0008「模拟集中器会话帧日志与 AI 控制面接口」。本次不烧录、不修改固件、不下发任何配置采集任务或档案的命令。

## 1. 验收需求

- 将 Windows `COM19`（Silicon Labs CP210x，usbipd bus `6-1`）挂载到当前默认 WSL，确认 Linux 设备为 `/dev/ttyUSB1`。
- 按 `9600,E,8,1` 由应用中的 `simcon` 映射打开，不绕过 API 直接占用串口。
- 通过工作台 AI 控制面 `/api/ai/v1/simcon/*` 验证打开、单步下发、只收等待、会话和帧查询、审计以及关闭。
- 只将实际 API 响应、串口参数和帧日志作为结论依据；无法安全验证的业务场景如实保留为未验收。

## 2. 基线与执行设计

| 项 | 实际基线/设计 |
| --- | --- |
| 拉取基线 | `git pull --ff-only` 成功：`9147435 → 5c8de1f`；需求 0008 的提交 `a8f2b3e` 已在当前 `master` 历史中 |
| 配置映射 | `config/serial_ports.json` 既有 `simcon`：`COM19 ↔ /dev/ttyUSB1`，`9600,E,8,1`；本次未修改配置 |
| WSL 映射 | 以管理员权限 `usbipd bind --busid 6-1`，再以当前默认 WSL `usbipd attach --wsl --busid 6-1` |
| API 服务 | 临时源码态工作台仅监听 `127.0.0.1:8791`；使用内存授权（`simcon:send`、`simcon:read`、`status:read`，资源 `simcon`），结束后关闭服务并删除 token |
| 主动下发边界 | 仅下发只读查询 `10H-F4`；不执行用例中会改写任务/档案的 `11H-*` 步骤；所有 responder 均禁用 |
| 被动接收边界 | 只收等待 `06H-F230` 8 秒；不发送确认帧 |

## 3. 映射验收

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| Windows 端设备 | 通过 | `usbipd list`：bus `6-1`、`10c4:ea60`、`Silicon Labs CP210x USB to UART Bridge (COM19)`，最终状态 `Attached` |
| WSL 端节点 | 通过 | 新增 `/dev/ttyUSB1`（major/minor `188,1`），其 USB 属性为 `ID_VENDOR_ID=10c4`、`ID_MODEL_ID=ea60` |
| USB 拓扑 | 通过 | `/sys/devices/platform/vhci_hcd.0/usb1/1-3/1-3:1.0/ttyUSB1`；同时保留既有 COM4 对应 `/dev/ttyUSB0` |
| 参数选择 | 通过 | API `POST /api/ai/v1/simcon/open` 的 `port_identity` 回显 `COM19`、`/dev/ttyUSB1`、`9600/E/8/1` |

## 4. AI API 实测

会话 ID：`sc-20260829-195154--dev-ttyUSB1`
帧日志：[sc-20260829-195154--dev-ttyUSB1.jsonl](/01-workfile-ai/01-zzt/ZZT_SELF/data/logs/simcon/sc-20260829-195154--dev-ttyUSB1.jsonl)

| API/动作 | HTTP 与接口结论 | 实机/业务结论 |
| --- | --- | --- |
| `GET /api/ai/v1/status` | `200` | 工作台 AI 控制面可达 |
| `POST /simcon/open` | `200`，`open=true`，映射 ID=`simcon` | 串口以预期 COM19 参数成功打开 |
| `GET /simcon/session` | `200`，当前会话落盘路径正确 | 初始 `tx=0/rx=0`，会话可查询 |
| `POST /simcon/step`：`10H-F4` 路由状态查询 | `200`，operation 状态 `succeeded`；`run_id=manual-20260829-195154-accdb8`，帧范围 `[1,2]` | **未通过**：收到真实上行否认帧，非场景期望的 `10H-F4` 应答 |
| `POST /simcon/step`：只收 `06H-F230` 8 秒 | `200`，operation 状态 `succeeded`，帧范围为空 | **未验收**：等待窗口没有主动上报；本步骤未发送任何帧 |
| `GET /simcon/frames`（关闭后） | `200`，仍可查询 `tx=1/rx=1/uplink=1` | 会话完成后帧日志仍可回查 |
| `POST /simcon/close` | `200`，`open=false` | 串口会话已关闭 |
| `GET /api/ai/v1/audit` | `200` | 存在 `simcon.open`、两次 `simcon.step`、`simcon.close`，均落账为 `succeeded` |

注意：AI operation 的 `succeeded` 表示 API 调用与操作记录成功；其内部 `step.result` 仍可能为 `fail`。本次 `10H-F4` 正属于该情形。

## 5. 帧级证据与判断

| 序号 | 方向 | AFN/Fn | 关键结果 |
| --- | --- | --- | --- |
| 1 | TX | `10H-F4` | `68 1B ... 10 08 00 88 16`；由语义化 `afn=10, fn=F4` 经 API 构帧并发出 |
| 2 | RX | `00H-F2` | `68 10 ... 00 02 00 0A D0 16`；解析为「确认/否认」，错误状态字 `10` = **主节点不支持此命令** |

由此可以确认 COM19→WSL→AI API→串口 TX/RX→帧日志的链路真实闭合；但不能确认 CCO 支持当前场景的 `10H-F4` 查询契约，也不能据此接受“路由状态查询”业务用例。

## 6. 代码回归与清理

- 定向回归：`PYTHONPATH=apps:libs .venv/bin/python -m pytest -q apps/workbench/test_ai_simcon.py libs/sim_concentrator/test_frame_journal_api.py libs/sim_concentrator/test_journal.py libs/sim_concentrator/test_runner_journal.py`
  - 结果：**43 passed**，1 个第三方 TestClient 弃用警告。
- 临时服务已退出，临时 API token 文件已删除；没有持久授权变更。
- 本次工作区改动仅为本验收记录；无固件代码、串口配置或远程提交改动。

## 7. 最终验收结论

| 范围 | 结论 |
| --- | --- |
| COM19 映射到 WSL | **通过** |
| `COM19 ↔ /dev/ttyUSB1, 9600,E,8,1` API 打开 | **通过** |
| AI API 授权、打开/关闭、操作审计 | **通过** |
| 单步语义下发、真实 RX、会话 JSONL 持久化与关闭后查询 | **通过** |
| `10H-F4` 路由状态业务响应 | **未通过**：CCO 明确返回「主节点不支持此命令」 |
| `06H-F230` 主动上报感知 | **未验收**：8 秒安全观察窗口内没有上报 |
| 会改写采集任务/档案的完整分钟采集用例 | **未执行**：超出本次无副作用验收边界 |

后续若要完成业务验收，需要先确认当前 CCO 固件/地区配置支持的查询 AFN/Fn，再在明确授权下选择相应的无副作用查询用例；本记录不把该部分视为通过。
## 8. 补充验收：用户修正后的档案流程（已完成）

用户确认正确的实机流程应为「查询模块地址 → 查询档案 → 添加档案」。该流程替代第 4 节中已知不支持的 `10H-F4` 路由状态查询作为业务前置。

| 顺序 | API 单步语义 | 期望 | 副作用与闸门 |
| --- | --- | --- | --- |
| 1 | `03H-F4`，无参数：查询主节点地址（模块地址） | 上行 `03H-F4` | 只读；不匹配即停止 |
| 2 | `10H-F2`，`start=0,count=16`：查询从节点/档案现状 | 上行 `10H-F2` | 只读；不匹配即停止 |
| 3 | `11H-F1`，`action=add, addr=080000000000, protocol=3`：添加 profile `sta698` 档案 | `00H-F1` 确认 | **写入 CCO**；仅在前两步均匹配时执行 |

三个请求已在离线语义化构帧层成功构建。实机使用 `simcon` 映射（`COM19 ↔ /dev/ttyUSB1, 9600,E,8,1`），关闭自动 responder；服务完成后关闭串口并保留本轮独立会话帧日志。

执行结果见 §8.2。
### 8.1 协议修正（执行前）

`03H-F12` 是**模块 ID**查询，不能作为“查询模块地址”使用；刚才该请求已真实下发但 5 秒无回应，按闸门停止，未进入档案查询和添加步骤。

本轮将第 1 步修正为 `03H-F4`（查询**主节点地址**，下行无数据单元，期望上行 `03H-F4` 的 6B 地址）。后续步骤保持：`10H-F2(start=0,count=16)` 查询从节点档案，随后才允许 `11H-F1(add,080000000000,protocol=3)` 添加档案。
### 8.2 实机结果：修正流程未越过第一道查询闸门

| 尝试 | 实际 TX/RX | 闸门结果 |
| --- | --- | --- |
| `03H-F12`（最初误用为模块地址，实际为模块 ID） | 已发送 1 帧，5 秒内 `rx=0` | 停止；`10H-F2` 与 `11H-F1` 未执行 |
| `03H-F4`（已修正为主节点地址） | 已发送 `68 1B ... 03 08 00 7B 16`，5 秒内 `rx=0` | 停止；`10H-F2` 与 `11H-F1` 未执行 |

正确命令 `03H-F4` 的本轮会话 ID 为 `sc-20260829-221455--dev-ttyUSB1`，帧日志：[sc-20260829-221455--dev-ttyUSB1.jsonl](/01-workfile-ai/01-zzt/ZZT_SELF/data/logs/simcon/sc-20260829-221455--dev-ttyUSB1.jsonl)。该会话 API 打开、单步操作、帧查询、审计与关闭均返回成功；但 `step.result=fail`，原因是未收到业务应答。

**本轮结论**：COM19→WSL→API→CCO 的下发路径已记录，但“查询模块地址”无法收到 CCO 回应，故不能安全进入查询档案或添加档案。没有调用 `10H-F2`，没有调用 `11H-F1`，档案未改变；串口已关闭，临时 API 服务和 token 已清理。
## 9. 通信参数同步重试矩阵（已完成）

用户要求：首次通信无响应时，应同步切换波特率与校验方式确认当前交互参数。对同一只读 `03H-F4` 主节点地址查询，逐次以 AI API `simcon/open` 的显式参数打开并关闭 COM19：

| 次序 | baudrate | parity | bytesize/stopbits | 判定 |
| --- | ---: | --- | --- | --- |
| 1 | 9600 | E | 8/1 | 收到并匹配 `03H-F4` 即为有效 |
| 2 | 9600 | N | 8/1 | 同上 |
| 3 | 115200 | E | 8/1 | 同上 |
| 4 | 115200 | N | 8/1 | 同上 |

每次操作仅使用已确认的 `/dev/ttyUSB1`（COM19）并禁用 responder；不会发送波特率设置命令，也不会进入档案查询/添加档案。每一轮结束立即关闭串口，会话 JSONL 作为独立证据保存。执行结果见 §9.1。
### 9.1 实机结果：四组合均未产生 RX

本节仅对只读 `03H-F4` 主节点地址查询进行参数探测。每组均由 AI API 的 `simcon/open` 显式传入参数、禁用 responder、查询会话帧日志后关闭串口；每次等待约 5 秒。

| 配置 | 实际收发 | 业务结果 | 会话 ID |
| --- | --- | --- | --- |
| `9600,E,8,1` | TX `03H-F4` 1 帧，`rx=0` | `step.result=fail`；已关闭 | `sc-20260829-223112--dev-ttyUSB1` |
| `9600,N,8,1` | TX `03H-F4` 1 帧，`rx=0` | `step.result=fail`；已关闭 | `sc-20260829-223117--dev-ttyUSB1` |
| `115200,E,8,1` | TX `03H-F4` 1 帧，`rx=0` | `step.result=fail`；已关闭 | `sc-20260829-223123--dev-ttyUSB1` |
| `115200,N,8,1` | TX `03H-F4` 1 帧，`rx=0` | `step.result=fail`；已关闭 | `sc-20260829-223128--dev-ttyUSB1` |

四轮的 `simcon.open`、`simcon.step`、`simcon.close` API 操作和审计均成功；这只表示端口可被打开、请求可发送且会话可关闭，不表示查询业务成功。四轮的业务结果均为 `step.result=fail` 与 `rx=0`。

**参数判断**：这四次 `03H-F4` 探测都没有期望应答，因此不能仅凭该命令在 `9600/115200 × E/N` 中选出新的有效组合。第 4 节已经在 `9600,E,8,1` 下收到过真实协议否认帧 `00H-F2`，故它仍是本次唯一具有双向帧证据的工作参数；但 `03H-F4` 在该参数下仍无回应，表明当前阻塞不能排除命令支持、地址/链路状态或实机处理时序因素。

本轮没有调用 `10H-F2`，没有调用 `11H-F1`，档案未改变。COM19 保持映射至 WSL，但四个 API 串口会话均已关闭；临时服务与 token 已清理。后续如继续实机诊断，应以 `9600,E,8,1` 为已有双向证据的基线，并另行确认 `03H-F4` 的设备支持条件。
