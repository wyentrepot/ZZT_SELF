# 侦听台改造项目任务安排

> 本文只定义任务 1～6、状态、优先级、阻塞项与验收出口。需求和设计正文分别引用[总需求](02-总需求.md)与[骨架设计](03-骨架设计.md)。

## 1. 状态口径

- `已完成`：验收出口有可复现证据。
- `进行中`：已有实际变更，尚未满足全部出口。
- `待开始`：尚未进入实现。
- `阻塞`：缺少外部决策、硬件、协议结论或依赖。

## 2. 总览

| 任务 | 优先级 | 状态 | 目标出口 |
|---|---:|---|---|
| 任务 1：冻结验证契约 | P0 | ✅ 已完成 | 领域模型、状态机、用例包和 API 契约通过评审并有单测（`pytest libs/test_automation` = 51 passed） |
| 任务 2：打通最小证据闭环 | P0 | 进行中（主体已通） | 补齐 Run 级 `inconclusive`、规范 Artifact 及可下载审计产物 |
| 任务 3：接入三源与迁移资产 | P1 | ✅ 已完成 | 三源统一接入，GW-CASS 关键用例迁移，引擎本体 Evidence 化收口 |
| 任务 4：完善统一工作台与交付 | P1 | ✅ 已完成 | 单入口、证据下钻、取消 Run、运行恢复、降级、Windows 打包验收全部通过（B-03 解除）；仅剩真机串口实测（任务 6 已屏蔽） |
| 任务 5：协议专项与质量加固 | P2 | 进行中（协议/性能/异常恢复已完成） | 协议待确认项关闭，性能与可靠性达到验收要求；OAD/OI 覆盖后续 |
| 任务 6：Windows 串口网关与 WSL 虚拟串口 | — | ⛔ 已屏蔽（不排期） | 四类入口、XMODEM、独占、断线和日志验收通过（恢复时启用） |

> **当前推进重点**：任务 3、4 已收口；任务 5 继续推进 OAD/OI 覆盖与长期运行验证。设计契约核查发现任务 2 仍缺 Run 级 `inconclusive`、规范 Artifact、统一错误响应和 workbench 状态模型对齐，这些是 P0/P1 收口项，详见[设计契约偏差核查](_archive/2026-08-18/13-设计契约偏差核查.md)。

## 3. 任务明细

### 任务 1：冻结验证契约

- 范围：CasePackage、Run、Evidence、AssertionResult、Report、Artifact、SourceAdapter、资源租约。
- 当前：**已完成**（2026-08-17）。领域模型在 `libs/test_automation/models.py`；新增 `fingerprint.py`（不可变指纹）、`state_machine.py`（Run 状态机，终态不可回退）、`adapters.py`（SourceAdapter）、`evidence_store.py`（证据追加/冻结）、`resource_lease.py`（资源互斥）；`models.py` 补 `Report.from_dict`。
- 阻塞：无。
- 验收出口：模型序列化、状态迁移、用例指纹、证据追加/冻结、资源互斥均有自动化测试。
- 证据：`pytest libs/test_automation` = **51 passed**（test_models_serialization / test_state_machine / test_fingerprint / test_evidence_store / test_resource_lease / test_adapters）。

### 任务 2：打通最小证据闭环

- 范围：首个安徽分钟采集用例、证据窗口、结构化断言、报告归档、基础 API。
- 当前：**主体已通、契约未完全闭环**（2026-08-18 复核）。真机已验证 COM23 烧录、COM24 交互（7/8）、698 主动上报（06H-F230 4/4）及重复执行；分钟采集断言已移植到 `libs/minute_assert/`。Run 已能汇总并冻结三源 Evidence，Report 已能内嵌证据下钻。
- 阻塞：~~E2/E3/E4 部分协议知识点待确认~~ → ✅ 已解除（2026-08-17，B-01，见 §4）。首版只使用已确认字段并显式标记未知项。
- 验收出口：固定输入结果一致；缺失必要来源为 `inconclusive`；报告可追到原始帧/日志；关键产物登记为带 SHA-256 的 Artifact 并可下载。
- 已有证据：真机过程见[待完成任务清单](_archive/2026-08-18/07-待完成任务清单.md) §3.1；断言移植见[分钟采集断言移植记录](_archive/2026-08-18/10-分钟采集断言移植记录.md)；自动化覆盖位于 `libs/minute_assert/test_*.py` 与 `apps/workbench/orchestration/test_evidence.py`。据报另一开发环境可能有 `docs/_evidence/2026-08-04-minute-collect-analysis.txt`，当前 D 盘检出未同步该文件，故标记为“跨环境待核验”，不判定其存在或不存在。
- 未闭环：workbench Report 仅有 `pass/fail`，必要来源缺失时尚不能输出 `inconclusive`；`artifacts` 仍是文件路径字符串，无规范 Artifact、SHA-256 清单和下载 API。这两个结论来自当前代码，不依赖跨环境证据文件是否已同步。

### 任务 3：接入三源与迁移资产

- 范围：侦听台帧、loghooks 事件、模拟集中器步骤接入；GW-CASS 用例语义与资产迁移。
- 当前：**已完成（2026-08-17 收口）**。已完成：① `libs/minute_assert/`（分钟采集日志断言）；② **GW-CASS 用例迁移**（`libs/gw_cass_migrate/`，97 用例→CasePackage，302 断言）；③ **三源 Evidence 适配**（`libs/test_automation/sources.py`：ListenerFrameAdapter 帧→Evidence、SimConcentratorAdapter 步骤→Evidence、LoghooksEventAdapter 事件→Evidence）；④ **Run 级三源编排**（`apps/workbench/orchestration/evidence.py` + `RunExecutor` 接入：monitor 事件/stimulus 步骤/listener 帧 → 同一 run 级 EvidenceStore，串口资源租约 + Report `evidence_index`/`evidence_frozen`/`sources.listener`）；⑤ **loghooks 引擎本体 Evidence 化**（`libs/loghooks/engine.py`：`Event.to_evidence()` 字段无损转换 + `Engine(on_event=)` 可插拔发射器，延迟 import 保持引擎与 test_automation 解耦；`runner._scan_logs` 直接收集完整 Evidence，不再走有损 dict 路径）。
- 阻塞：~~真实串口和设备测试需要 Windows 硬件环境~~ → ✅ 已解除（2026-08-17，B-02，见 §4）。
- 验收出口：~~一个 Run 同时消费三源证据~~ → ✅ 已达成（`collect_three_source_evidence` + RunExecutor 端到端测试）；✅ 资源冲突可预测（`acquire_serial_lease` 独占冲突 / shared 共存）；⏳ 迁移用例与旧工具结果一致（既有验证项）。
- 证据：`pytest libs/gw_cass_migrate` = 10 passed；`pytest libs/test_automation` = 58 passed（含三源适配 7）；`pytest apps/workbench/orchestration/test_evidence.py` = 21 passed（三源 Run 编排 17 + 引擎本体 Evidence 化 4）；`pytest libs/test_automation libs/sim_concentrator apps/workbench libs/loghooks` = 187 passed 无回归；迁移实现与可复现样例由 commit `3dcd2f7`、`libs/gw_cass_migrate/test_migrator.py` 和[迁移记录](_archive/2026-08-18/11-GW-CASS用例迁移记录.md)共同留证。据报可能位于另一环境的 `docs/_evidence/gw_cass_migrated_cases.json` 当前未同步到 D 盘检出，作为跨环境补充产物待核验。

### 任务 4：完善统一工作台与交付

- 范围：验证 UI、运行恢复、证据下钻、错误降级、一键启动和打包。
- 当前：`apps/workbench` 正在开发，listener/module_log 已有独立入口；统一交付仍需系统验收。
  - ✅（2026-08-17，WSL 内）**Run 接入 COM4 侦听台串口三源闭环**：`evidence.load_listener_frames_from_index()` 从 listener 帧索引库（`apps/listener/runtime/log_index.sqlite3`，COM4 采集落库）读帧注入三源 Run，未注入 `listener_frames` 时自动读取；库缺失优雅降级不阻断 Run。`pytest apps/workbench/orchestration/test_evidence.py` = **17 passed**；`libs/test_automation libs/sim_concentrator apps/workbench` = **156 passed** 无回归。
  - ✅（2026-08-17）**验证 UI 证据下钻**：`evidence_detail()` 输出完整证据明细（按 source 分组含 payload/metadata/raw_ref，写入 Report）；`workbench.html` Run 执行后自动加载 report 渲染「④ 证据下钻」面板，按 source 分组、`<details>` 可展开原始 payload/metadata（FR-6 要求）。`test_evidence.py` = **26 passed**（新增 detail 3）；全量 `pytest libs apps` = **580 passed** 无回归。
  - ✅（2026-08-17）**取消 Run（FR-6 支持取消）**：`RunExecutor.submit()` 后台线程异步执行（`POST /api/run` 立即返回 running，前端轮询状态）；`cancel()` 协作式取消（`threading.Event` 标志，`_run_steps` 各步骤间检查，落 CANCELLING → CANCELLED 终态）；新增 `POST /api/run/{run_id}/cancel`；`_executor()` 单例化修复（避免 submit/cancel 不同实例）。前端 `workbench.html` 运行中显示「取消 Run」按钮 + 状态轮询，cancelled 徽标；被取消 Report 标注 `run.cancelled`。`test_app.py` + `test_evidence.py` = **42 passed**（新增 cancel 5）；全量 `pytest libs apps` = **585 passed** 无回归。
  - ✅（2026-08-18）**运行恢复（FR-6 刷新后恢复）**：前端 `restoreLastRun()` 页面加载时取最近 Run——终态渲染结果+证据+徽标，运行中继续 `pollRun` 恢复实时状态/取消按钮；失败静默降级。配套修复 `store` created_at/updated_at 毫秒级时间戳（原秒级导致同秒多条 `list_runs` 排序不稳定，恢复可能取错 Run）。`TestRunRestore` 1 用例；全量 **588 passed** 无回归。
  - ✅（2026-08-18）**打包静态资源完整性（B-03 自动化部分）**：新增 `apps/workbench/check_assets.py` 打包门禁——关键资产存在/非空、HTML 引用 `/static/` 资源完整性、空文件检测（`--strict` 退出码非 0 供 CI）；`test_app.py` 加 2 测试（引用资源全 200 + 关键页面全 200，含子应用 pages）。`check_assets --strict` = **12 资产通过**；启动冒烟由 `test_app.py` 15 passed 覆盖（应用可构建 + 路由全 200）。
  - ✅（2026-08-18）**打包验收（B-03）**：真机 Windows 环境 `PyInstaller` 打包 workbench onedir → `dist/工作台/工作台.exe`（7.8MB）+ `_internal/static`（12 资产全）+ `_internal/dll/bin/Debug/GwHPLCAnalysis.dll` + scenarios（4 场景）+ loghooks rules；headless 启动冒烟 `tools/scripts/smoke_test_workbench_packaged.py` = **9/9 PASS**（health=workbench、首页/静态资源 200、platform-version、module-serial/listener 子应用代理 200、exe 同目录生成 runtime/）。B-03 阻塞解除。
  - ⏳ 待真机串口：COM4=/dev/ttyUSB0 实测确认（见 `docs/08` §1，需真实集中器/电表）。
- 阻塞：~~打包依赖 .NET DLL 与静态资源完整性（B-03）~~ **已解除**（2026-08-18 真机打包+冒烟通过）。剩余阻塞仅真机串口实测（B-04，任务 6 已屏蔽）。
- 验收出口：干净 Windows 环境可启动；刷新后恢复 Run；DLL/串口缺失不阻断无关能力；发布物无开发机路径。

### 任务 5：协议专项与质量加固

- 范围：OAD/OI 覆盖、分钟采集待确认项、大文件性能、异常恢复、回归与发布检查。
- 当前：**进行中（2026-08-17）**。已完成：① **分钟采集问题清单 7 项**——E3 协议类型/电表类型位宽（3+2+3）、冻结时刻小端 BCD、E2 多余方向字段、10376 补 00E2/E3/E4 注册（确定性 bug），E2 上行应答（§2.2）、E4 并发抄读格式展开（§4.1）实现；② **B-01 残留 3 项关闭**——E4 result≠0 断言口径、E2 删除应答判定固化测试，E3 位宽统一随①解决；③ **性能基线可复现**——`apps/listener/test_perf_baseline.py` 5 条查询路径基线（5万行实测浅翻页6.7ms/keyset3.8ms/时间范围3ms/query14ms/nid27.5ms，远优于旧基线）；④ **异常恢复+发布冒烟**——RunExecutor 中途异常→failed 终态+report 下钻，无串口环境冒烟跑通。**后续项**：OAD/OI 覆盖（18→515，3.5%，用户决定单独立项）；真机验证残留（reset_index 自动清库、前端轮询瘦身、LOG 按天切分，标真机后实施）。
- 阻塞：~~协议口径需产品或协议负责人确认~~ → ✅ 已解除（2026-08-17，B-01，见 §4）。
- 验收出口：待确认项有结论与测试；性能基线可复现；异常恢复、长时间运行和发布冒烟通过。
- 证据：`pytest libs/parser_lib libs/minute_assert` = 137 passed（含 E2/E4 新增）；`pytest libs/minute_assert` = 26 passed（B-01 残留固化）；`pytest apps/listener/test_perf_baseline.py` = 5 passed；`pytest apps/workbench/orchestration/test_evidence.py` = 23 passed（含异常恢复/冒烟）；全量 `pytest libs apps` = 577 passed / 66 skipped 无回归。

### 任务 6：Windows 串口网关与 WSL 虚拟串口

> ⛔ **已屏蔽（2026-08-17 用户决定）**：暂不实现，从需求范围屏蔽；恢复执行前不推进实现、不占实机/烧录窗口。本文保留任务定义与验收出口作为需求档案。
>
> 相关档案：`reqs/0003-windows-uart-bridge/`（REQS / TODO / DONE）、`docs/_archive/2026-08-18/05-Windows串口转TCP与WSL虚拟串口方案.md`、`docs/_archive/2026-08-18/06-需求0003实施调研与计划.md`。恢复执行时从 TODO 阶段 0 建功能分支继续。

- 范围：新建 `D:\019-wy-tool\uart_to_tcp`；新增 WSL local/windows_tcp；接入 listener、CCO、STA、sim_concentrator；XMODEM 在 Windows 执行。
- 当前：方案 A、需求和逐步计划已确认，尚未实现代码。
- 阻塞：终验需要真实 COM、CCO/STA 和固定固件。
- 验收出口：Windows 双击启动；严格独占；四类入口工作；断线不恢复；共享路径 XMODEM 成功；双方日志可关联。
- 执行入口：[`reqs/0003-windows-uart-bridge/TODO.md`](../reqs/0003-windows-uart-bridge/TODO.md)。

## 4. 当前阻塞清单

| 编号 | 阻塞项 | 影响 | 状态 | 解除条件 |
|---|---|---|---|---|
| B-01 | 安徽分钟采集 E2/E3/E4 字段口径未完全确认 | 任务 2、5 | ✅ 已解除（2026-08-17） | 协议负责人确认并补回归样例 |
| B-02 | 真实串口/设备测试环境并非持续可用 | 任务 3、4 | ✅ 已解除（2026-08-17，用户确认环境可持续运行） | 提供可预约环境及固定设备配置 |
| B-03 | Windows 打包依赖 DLL 与静态资源完整性 | 任务 4 | ✅ 已完成 | 静态资源完整性门禁（check_assets，12 资产）+ 真机 PyInstaller 打包（dist/工作台/，含 C# DLL/scenarios/loghooks）+ headless 启动冒烟 9/9 通过（health/静态页/子应用代理/runtime）；证据见 `tools/scripts/smoke_test_workbench_packaged.py` |
| B-04 | 网关终验依赖真实 COM、CCO/STA 和固定固件 | 任务 6 | 挂起（任务 6 已屏蔽） | 提供设备、串口分配、固件路径和烧录窗口 |

> 注：B-04 属已屏蔽任务 6（2026-08-17），暂停期间不解除；恢复任务 6 时再按上表解除条件处理。
>
> 阻塞解除记录（2026-08-17）：
> - **B-01 解除**：E2/E3/E4 字段口径经另一开发环境的 C 侧权威源码（`/home/H_CCO/001/cco/protocol/aps/src/aps_stack.c`、`aps_stack.h`）和真实帧回归样例交叉验证；仓库内可复现证据为 commit `e07b1d7`、`libs/parser_lib/adapters/adapter_dualmode/tests/test_minute_collection.py` 与[`安徽集中器交互报文.md`](_references/protocols/安徽集中器交互报文.md)。另一环境报告的 `docs/_evidence/2026-08-17-698-meter-collect-cco-debug.log` 当前未同步到 D 盘检出，保留为跨环境证据路径，不据此否定其存在。
> - **B-02 解除**：用户确认真实串口/设备测试环境可持续运行（2026-08-17）。

## 5. 维护规则

状态变化必须记录验收证据位置；不得在本文重新定义需求字段或架构契约。历史计划与问题清单见[`_archive/2026-08-16/`](_archive/2026-08-16/)。

## 6. 需求追踪矩阵

| 需求 | 设计契约 | 任务 | 当前证据/待补证据 |
|---|---|---|---|
| FR-1、FR-7 | 骨架 §2 listener/parser、§8 日志索引 | 2、3、5 | 真机与分钟断言主体已验证；任务 3 三源 Run 证据位于 `apps/workbench/orchestration/test_evidence.py`；任务 2 仍待补 `inconclusive` 与规范 Artifact |
| FR-2 | 骨架 §2 module_log、§8 串口/XMODEM | 3、4 | 现有代码：`apps/module_log/`；待补干净机与真实串口验收 |
| FR-3 | 骨架 §2 loghooks、§5 适配器 | 3、5 | 任务 3 已收口：`libs/minute_assert/`（分钟采集日志断言 22 passed）+ LoghooksEventAdapter（事件→Evidence）+ Run 级三源编排 + **引擎本体 Evidence 化**（`Event.to_evidence()` + `Engine(on_event=)`，字段无损） |
| FR-4 | 骨架 §2 sim_concentrator、§8 端口/任务 | 3、5 | 任务 3 已落地：GW-CASS 用例迁移完成（97 用例→CasePackage）+ SimConcentratorAdapter（步骤→Evidence）+ Run 级三源编排（stimulus 步骤→EvidenceStore） |
| FR-5 | 骨架 §3～§6、§9 | 1、2、3 | 任务 1 领域契约与任务 3 三源 Run 编排已落地；任务 2 尚缺 Run 级 `inconclusive`、Artifact 登记/下载和统一错误格式 |
| FR-6 | 骨架 §1、§6～§7 | 4 | ✅ 完成：`apps/workbench/` 证据下钻、取消 Run、运行恢复、静态资源完整性、真机 DLL 打包验收全部落地（588 passed + 打包冒烟 9/9） |
| FR-8、NFR-2～8 | 骨架 §7～§10 | 4、5 | 任务 5 性能基线已补（`apps/listener/test_perf_baseline.py` 5 passed，实测远优于旧基线）；异常恢复+发布冒烟已补（`test_evidence.py` 23 passed）；待补 CI、真机长时间运行 |
| CR-1（原 FR-9） | 骨架 §11、历史专项方案 | 6 | 已屏蔽，不属于当前 FR 验收；恢复时重新评审并分配正式 FR 编号 |

> 注：任务 6 已屏蔽（2026-08-17），对应 CR-1 不参与当前范围的“已验收/未验收”统计。

“现有代码”只证明任务已开始，不代表验收完成。状态转为“已完成”时必须把本列替换为可复现的测试命令、报告或 Artifact 路径。
