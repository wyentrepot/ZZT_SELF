# 待完成任务清单与交付说明

> 本文档登记 ZZT_SELF 当前交付状态、已完成项与后续待完成任务。
> 依据《04-任务安排.md》与安徽集中器交互 task 实施进度。

## 1. 已完成交付（已提交 origin/master）

| Commit | 内容 |
|---|---|
| `56b8061` | sim_concentrator CCO 本地协议（单 68 帧）支持：FN 编码/构帧/切帧/解析、serial_io 双格式切帧+历史记录、responder 统一回 00H-F1、matcher 本地帧匹配、runner recv_only/expect_history + responder 接入闭环 |
| `911581d` | 安徽集中器交互 task 文件：`apps/workbench/scenarios/tasks/anhui_minute_collect.json`（COM24/9600，6 主动下发 + 3 主动上报验证，responder 统一回确认） |
| `7545261` | 场景模板接入：`_run_stimulus` 支持相对 scenarios 目录解析 task |
| `fd24c2e` | CCO 本地协议单测 `test_ccol_local.py`（11 测试） |
| `ec3680d` | task 验证单测 `test_anhui_task.py`（5 测试） |
| `fbe500b` | 归档安徽集中器交互报文文档 |

**测试**：`pytest libs/sim_concentrator/` = 46 passed（含 16 个新单测），无回归。

## 2. 用户侧新增（WSL 本地，待同步 D 盘）

- `7aa646c`：req-0003 Windows 串口网关与 WSL 虚拟串口——登记方案/需求/任务与实施调研

## 3. 待完成任务

### 3.1 真实硬件跑通（plan_step_05，当前 in_progress）

**前置已就绪**：
- [x] COM23（烧录口）、COM24（模拟集中器口）串口存在
- [x] 固件可访问：`\\wsl$\...\firmware\flash_cco_...bin`（2MB 生产烧录）、`iap_cco_...bin`（464KB IAP 升级）
- [x] 烧录实现确认：`xmodem_flash.py`（reboot → bootloader → download <slot> → Y → XMODEM-1K，波特率 115200，resolve_bin_path 支持 WSL 路径）
- [x] 执行环境打通：`.venv` python 用 temp 明文代码可 import 完整 sim_concentrator

**待执行（涉及真实设备固件写入，不可完全回退）**：
- [ ] 确认烧录用固件文件与 slot（iap_cco_...bin 或 flash_cco_...bin）——固件已确认存在：`/home/H_CCO/001/cco/firmware/`（flash 2MB / iap 464KB / upgrade .dat）；README：iap 走串口转 TTL 升级，flash 为生产烧录；slot 待真机确认（默认 0）
- [ ] COM23 执行 XMODEM 烧录，验证 Image download OK——命令见《08-真实硬件验证手册.md》，可复用 module_log 烧录功能：`python apps/module_log/flash_module.py --port COM23 --bin <iap_...bin> --slot N`
- [ ] COM24 用 `anhui_minute_collect.json` task 执行交互（execute_task 真实串口）
- [ ] 验证主动下发 send+expect、主动上报 recv_only/历史查询、responder 回确认
- [ ] 重复执行验证结果一致性（可复现）
- [ ] 产出报告/证据，写回《04-任务安排.md》任务 2 状态列

> 前置：用桌面 `串口映射.bat` 将 COM23/COM24 映射到 WSL（见 §5 新增交付）。

## 5. 本次交付（2026-08-17 会话）

### 5.1 桌面串口映射脚本（WSL 前置条件）

- **文件**：`C:\Users\A24006872\Desktop\串口映射.bat` + `uart-map.ps1`
- **功能**：双击弹出选项——`[1]` 映射串口到 WSL（usbipd attach 全部 COM + WSL 内 modprobe ch341/cp210x/cdc_acm）／`[2]` 恢复串口到 Windows（detach）／`[3]` 查看状态
- **实测**：4 个串口（COM3 CH343 / COM4+COM24 CP210x / COM23 CH340）映射后 WSL 出现 `/dev/ttyACM0` + `/dev/ttyUSB0/1/2`；恢复后 COM 全部回 Windows
- **技术说明**：依赖 usbipd-win 4.4.1（已装、服务运行）+ WSL2 内核 usbip 客户端；CH343 需 `cdc_acm` 模块

### 5.2 3.2 代码待办完成

- 提交 `4d41975`：`_auto_reply` 日志化 + recv_only/expect_history 语义锁定（+7 测试，53 passed）

### 5.3 工程化

- 提交 `ef91d57`：`reasonix.toml` 移除出版本库 + `.gitignore`
- D 盘工作区 `D:\12-wy-share-workfile\01-zzt\ZZT_SELF` 同步到 origin 最新（`4d41975`）

### 5.4 真实硬件验证手册

- 《08-真实硬件验证手册.md》：烧录 + 交互验证 checklist（待用户配合执行）

### 3.2 代码待完善项

- [x] runner 的 recv_only 步骤：expect 不匹配时连续接收直到超时（当前对非 recv_only 首帧不匹配即判 fail，recv_only 会继续等）——**已确认已实现**，补语义注释 + 2 测试（`test_recv_only_skip_unmatched_until_match` / `test_recv_only_unmatched_all_timeout`）
- [x] expect_history 与 recv_only 组合的边界：历史帧被 expect 消费后不重复验证的语义确认——**已确认**：rx_history 只读累积、匹配不消费、可安全叠加 recv_only，补注释 + 3 测试（`test_expect_history_*` / `test_recv_only_with_expect_history_combo`）
- [x] `_auto_reply` 对 responder 异常静默吞掉，可考虑记录日志——**已实现**：改为 `logger.exception`（不中断流程），补 2 测试（responder 异常 / 发送失败）
- [ ] task 中 11H-F231 配置帧的 Ln 字节数（文档 2 字节 vs CCO parse 可能差异）需真机验证确认——**文档已确认 Ln=2 字节**，CCO 实机 parse 差异需 3.1 真机验证（见《08-真实硬件验证手册.md》第 4 步）
- [ ] 场景模板 `minute_collect.json` 的 `monitor` 规则集与本地单 68 事件关联（当前 monitor 仍走 loghooks 离线扫描）——**用户决定真机验证后再实施**（涉及编排，避免盲改）

> 3.2 代码改动已提交 `4d41975`，`pytest libs/sim_concentrator/` = 53 passed 无回归。

### 3.3 工程化待办

- [x] D 盘工作区同步到 origin 最新——WSL 推送 `fbe500b..4d41975` + `ef91d57`，`D:\12-wy-share-workfile\01-zzt\ZZT_SELF` fast-forward 到 `4d41975`，工作树干净
- [x] `reasonix.toml` 本地配置决定是否纳入版本库——**用户决定从 git 移除并加入 .gitignore**（提交 `ef91d57`）
- [ ] 真实硬件验收通过后更新《04-任务安排.md》任务 1/2/4 状态与证据路径（待 3.1 完成）

## 4. 执行纪律

- 涉及真实设备烧录/外发前必须确认硬件连接与用户授权
- 所有代码改动先写单测再实现，pytest 全绿才提交
- WSL 仓库只在默认 WSL 写入/Git/测试；Windows 硬件运行用 Windows 工具链
- 不顺手改协议、loghooks、编排；每阶段评审通过再继续
