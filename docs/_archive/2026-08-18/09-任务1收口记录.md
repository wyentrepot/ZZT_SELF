# 任务 1 收口记录（2026-08-17）

> 本记录供查阅：A1→A2→A3 完成内容、验证证据、环境搭建、回归结论、决策记录。

## 1. 背景

- 用户质疑"任务 1/2 不是做完了吗"→ 核对发现：`libs/test_automation/models.py` 有领域模型但**零测试**、引用的 `fingerprint.py`/`state_machine.py` 不存在（孤儿代码）、`SourceAdapter` 未实现、`EvidenceStore`/`ResourceLeaseManager` 未实现。
- 用户决定：WSL 侧完成 A1→A2→A3 并提交 git、更新任务进度、留记录文档（自主执行，下班时间）。

## 2. 环境搭建（WSL 侧）

- WSL 发行版：Ubuntu-22.04（root 用户）
- 仓库真实路径：`/01-workfile-ai/01-zzt/ZZT_SELF`（ext4 原生，符合项目"WSL 只在原生路径写/Git/测试"纪律）
- 系统原缺 `python3.10-venv` → `apt-get install python3.10-venv`
- 创建 `.venv`（python3.10.12）+ 安装依赖：
  - `fastapi==0.141.1`、`httpx==0.28.1`、`pydantic==2.13.4`、`pyserial==3.5`、`pytest==9.1.1`、`uvicorn==0.52.3`
  - 注：`pythonnet` 是 Windows DLL 专属，WSL 不装（项目既有基线）。
- `.gitignore` 已含 `.venv/`、`__pycache__/`，不会误提交。

## 3. 完成内容（A1→A2→A3）

| 文件 | 内容 | 测试 |
|---|---|---|
| `libs/test_automation/fingerprint.py` | `case_fingerprint()`：model_dump → sort_keys+紧凑 JSON → sha256（与键顺序无关，内容变化指纹变化） | `test_fingerprint.py` 6 |
| `libs/test_automation/state_machine.py` | `RUN_TRANSITIONS` 迁移表 + `transition()`：合法迁移、终态不可回退、from==to 幂等 | `test_state_machine.py` 15 |
| `libs/test_automation/adapters.py` | `SourceAdapter` ABC（start/collect/stop/health）+ `AdapterError` + `AdapterHealth`（stop 幂等、错误统一类型） | `test_adapters.py` 7 |
| `libs/test_automation/evidence_store.py` | `EvidenceStore`：追加分配 stable id/run_id/sequence 单调、freeze 后拒绝追加（幂等）、只读 list | `test_evidence_store.py` 8 |
| `libs/test_automation/resource_lease.py` | `ResourceLeaseManager` + `ResourceConflictError`：串口独占、shared 共享、冲突拒绝、release 幂等 | `test_resource_lease.py` 7 |
| `libs/test_automation/models.py`（修改） | 补 `Report.from_dict`（原只有 to_dict） | `test_models_serialization.py` 8 |
| `libs/test_automation/__init__.py` | 包初始化 | — |

**合计：`pytest libs/test_automation` = 51 passed。**

契约来源：`docs/03-骨架设计.md` §3（数据模型）、§4（用例与执行流程）、§5（适配器协议）、§7（持久化/并发）、§8（状态机）、§9（全局约定）。

## 4. 验证证据

```text
pytest libs/test_automation                  → 51 passed
全量回归（listener/module_log/workbench/shared/parser_lib/sim_concentrator/test_automation）
  → 393 passed / 66 skipped / 13 failed
```

- 13 个 failed **全部为 WSL 侧既知 Windows 资源缺失**：
  - `GwHPLCAnalysis.dll` 不存在（DLL 仅 Windows 构建）
  - `启动工具.bat` 不存在（Windows 批处理）
  - `test_concurrent_meter_e2e` 依赖样本数据
  - `test_dll_python_meter_consistency` 依赖 DLL
- **grep 证实**：apps/shared/parser_lib/sim_concentrator 无任何文件 `import test_automation` → 本次改动不可能影响它们 → 零回归。
- 项目文档已记录 WSL 无 DLL 基线为"326 passed / 66 skipped / 9 DLL 失败"（`docs/_archive/2026-08-16/`），本次 13 失败即该类环境项。

## 5. 任务状态更新（docs/04-任务安排.md）

- 总览表：任务 1 → **✅ 已完成**（`pytest libs/test_automation` = 51 passed）
- 任务 1 明细：状态改已完成，附实现清单 + 证据命令
- 追踪矩阵 FR-5：更新为"任务 1 已达成…；任务 2、3 待闭环"
- 任务 2 仍"进行中"（报告写回、真机收尾、主动上报待电表数据未完成）

## 6. 决策记录

| 决策 | 选择 | 理由 |
|---|---|---|
| 环境位置 | WSL 原生路径建 `.venv` | 用户明确"wsl侧"；符合项目纪律（不通过 /mnt 或 Windows 工具写 WSL 文件） |
| 依赖范围 | fastapi/httpx/pyserial/uvicorn/pydantic/pytest | conftest 顶层 import `shared.infra` → fastapi；README 验证命令依赖全量 |
| pythonnet | 不装 | WSL/Linux 不支持，DLL 专属 Windows |
| `Report.from_dict` | 补实现 | 任务 1"模型序列化"出口需要 round-trip，原 models.py 缺 |
| EvidenceStore/ResourceLeaseManager | 新建在 `libs/test_automation/` | models.py 注释已声明其职责（append 分配 id/seq、资源互斥），文档 §7/§9 契约 |
| 13 个既有失败 | 不修 | 与本次改动无关（无引用 + Windows 资源缺失），属 WSL 环境基线，需 Windows+DLL 环境终验 |
| 提交拆分 | req-0003 屏蔽文档 与 任务1 代码 分开提交 | 语义独立，便于追溯 |

## 7. 未完成/后续

- 任务 2：报告写回《04》任务 2 状态列、真机收尾（重复执行一致性、Ln 字节确认、主动上报待电表数据）
- 任务 3：接入三源与迁移资产（待 B-02 环境）
- 任务 5：协议专项（待 B-01 解除）
- 任务 6：已屏蔽（2026-08-17）
- 全量测试的 13 个 WSL 失败项需在 Windows + DLL 环境终验（属既有基线）
