# 698.45 OAD 覆盖工作 · 归档说明（2026-08-28）

> 归档人：Reasonix（技术总监角色，派发与审核）
> 归档原因：用户决定"暂时不进行了，归档完成即可"。所有 OAD 相关工作产物归档于此，
> 活跃目录 `docs/协议/698.45-OAD覆盖/` 已清空，本目录为唯一保留副本。

## 一、本次工作脉络

1. **目标**：补充项目中未注册的 OAD（DL/T 698.45 协议对象标识），派发 hy3 / deepseek-v4-flash
   执行，Reasonix 做技术总监（派工+审核，不亲自做数据工程）。
2. **注册表**：`libs/parser_lib/adapters/adapter_698/metadata/oad.json`（外置 JSON 字典，
   `MetadataStore.lookup("698.45", oad_key)` 查名，增补条目即注册，不改代码）。
3. **标准基准**：DL/T 698.45 附录A（表A.1~A.14）共 **515 个 OI**。
4. **基线**：补全前 oad.json = 52 条 / 19 OI，覆盖 **3.5%**。

## 二、执行与中间状态（重要）

- 期间经 deepseek-v4-flash 分批补全，曾达到 **168 条 / 135 OI（26%）**，并做过 36 条错位修正。
- **但这些补全/修正成果最终未保留在磁盘**：当前 git 工作树 oad.json 仍为 **52 条原始基线**
  （git 无 oad.json 变更记录，.build_plain / worktree 副本均为 52 条）。168 条版本去向不明，
  用户决定不追回，以当前 52 条为准收尾。
- 对当前 52 条的**协议语义复核已完成**（见 `复核报告.md`）：结论 **36 条 OI↔名称错位 + 4 类附带问题**。
  - 最严重：`3000/3001` 系列（事件类=失压/欠压事件）被误标为"电能"；`2002/2003`（电压相角/电压电流相角）
    被误标为"频率/瞬时功率"；`6000/6001/6002`（采集档案配置/搜表）被误标为"事件对象"；`F100`（ESAM）
    被误标为"冻结电能"。
  - 修正派工单已生成（`修正派工单-36条错位.md`），但**修正未执行**（归档时已停止后台任务）。

## 三、工具改动（已落到工作树，未提交）

- `tools/scripts/analyze_oad_coverage.py`：
  1. 路径修复：OAD_JSON/DI_JSON 补 `libs/` 前缀（仓库迁移后过期）。
  2. 报告路径改为 `docs/协议/698.45-OAD覆盖/OAD覆盖分析报告.md`（原归档旧路径失效），并自动建目录。
  3. **OI 段权威分类**：因 md 源排版错乱（表边界不可靠、真实冻结 5004~5011 被漏提、参变量残留误归冻结类），
     改为按 OI 编号段判定类别（`_class_of_oi`），并补纯文本行匹配 + 名称清洗。
- pytest 回归：`pytest libs/parser_lib` = **116 passed / 62 skipped，无回归**。

## 四、归档文件清单

| 文件 | 内容 |
| --- | --- |
| `OAD覆盖分析报告.md` | 覆盖率分析（515 标准 / 当前 18 覆盖 / 3.5%），按类别缺失清单 |
| `任务派工单.md` | 初始任务规格（补全 515 OI、schema、验收） |
| `缺失OI派发清单.md` | 全量缺失 OI 明细（OI/名称/IC，按类别）——供日后继续补全用 |
| `复核报告.md` | 对当前 52 条的协议语义复核（36 条错位 + P01~P15 问题明细） |
| `复核派工单-52条.md` | 复核任务 prompt |
| `修正派工单-36条错位.md` | 修正 36 条错位的派工 prompt（未执行） |
| `文档一致性核查派工单.md` | D 类文档核查 prompt（发现 17 文档失效引用等，未处理） |

## 五、待续事项（日后恢复时）

1. **修正 36 条错位**（用 `修正派工单-36条错位.md` 派发），或决定是否追回 168 条补全版本。
2. **补全剩余 381 个缺失 OI**（用 `缺失OI派发清单.md`，按类别分批，单批 ≤ 100 条避免超时）。
   - 注意：真实冻结类 5004~5011（日/月/年冻结等）此前因 md 分类 bug 漏派，务必补。
3. **文档核查发现的问题**（17 文档失效引用、REQS-INDEX 缺 0005/0006 目录、16-AI操作指南过期）
   未处理，见 D 类核查输出。
4. 工具改动 `analyze_oad_coverage.py` 与归档目录均未提交 git，恢复时先 commit。

## 六、执行通道备忘

- codebuddy CLI：`codebuddy -p -y --model deepseek-v4-flash`（写权限需 `-y`；外部目录读取会被拦，
  数据基准用项目内文件）。hy3 单次大批量任务易超时无产物，建议拆批 ≤ 100 条。
