# 已完成需求

> **文档用途**：汇总本项目已完成的需求/功能，作为需求现状的「已完成」侧唯一入口。
>
> **维护方式**：新增完成项追加到对应分类；状态变更时从 `docs/需求管理/待完成需求.md` 移入本节并标注完成日期。
> **归档底稿**：`docs/需求管理/归档/任务交接需求与进度表.md`（历史会话记录）、`DECISIONS.md`（ADR 决策）。

---

## 一、核心解析能力（listener / parser_lib / shared）

| 编号 | 需求 | 验收/结果 | 状态 |
|---|---|---|---|
| R-01 | 解析国网 `FF 02` 侦听台外层帧 | 能识别外层、协议类型、RSSI、通道、CRC | ✅ 已完成 |
| R-02 | 解析 FCH 和 MPDU | 完整帧能展开 FCH、MPDU 结构；不足时保留部分结果并给出长度提示 | ✅ 基础完成 |
| R-03 | 支持大日志文件 | 不把整文件交给浏览器；顺序读取一次；页面只拉取分页摘要 | ✅ 已完成 |
| R-04 | 帧列表分页 | 每页最多 500 条，支持序号、时间、字段筛选 | ✅ 已完成 |
| R-05 | 单帧深度解析 | 点击帧后才调用完整 DLL 解析，显示解析链路和原始帧 | ✅ 已完成 |
| R-06 | 并发抄表帧 | 已对接 Python 深度解析并用真实大日志验收（200/200 帧递归 698.45）；数据单元循环与请求/响应关联 → 见待完成 | 🔶 主体完成 |
| R-07 | 测试和正式启动 | 测试模式自动加载小样本；正式模式由用户选择日志 | ✅ 已完成 |
| R-08 | 后续日志流接入 | 索引服务可替换为持续输入或增量追加 | ✅ 架构已预留 |
| — | 698/645 适配器 | `adapter_698`、`adapter_645`、`adapter_dualmode`、`adapter_10376` 覆盖主流帧格式解析 | ✅ 已完成 |
| — | 分钟采集识别 | `APP_PORT=0x11` + 报文 ID `00E2/00E3/00E4` 识别（禁止按字节串盲搜） | ✅ 已完成 |
| — | 分钟采集应用层富化 | E2/E3/E4 结构化字段、内嵌 645/698 递归解析 | ✅ 已完成 |
| — | 前端「应用层展开」标签页 | 并发抄表帧（0003）与分钟采集帧（E2/E3/E4）渲染 fields/items/nested 折叠树 | ✅ 已完成 |
| — | DLL 协议字段修正 | 报文 ID=3 偏移修复、电能表地址 5→6 字节、超时单位 ×100、方向字段等（V1.0.23） | ✅ 已完成 |

## 二、日志分析 / 数据源

| 需求 | 结果 | 状态 |
|---|---|---|
| 大文件索引 | SQLite 保存帧摘要和原始十六进制帧，支持进度和分页 | ✅ 已完成 |
| 文件选择器（免手动输入路径） | Windows 原生文件对话框（PowerShell STA `OpenFileDialog`），上次路径持久化，中文路径 UTF-8 Base64 编码修复 | ✅ 已完成 |
| NID 网络范围筛选 | 摘要 NID 列 + 帧浏览筛选 + 所有分析共享筛选，LIKE 通配符转义/输入校验/大小写规范化 | ✅ 已完成 |
| 串口实时采集 | COM19/115200 裸 7E 帧流切帧（HDLC 转义）、实时落盘 `data/logs/` + sqlite 入库 | ✅ 已完成 |
| 数据源二选一 | 串口监听 / 日志文件分析运行时互斥（409），切换清空重建索引 | ✅ 已完成 |
| 日志缓冲环形裁剪 | 后端内存 5000 行 + 前端 DOM 3000 行裁剪，防整夜 OOM | ✅ 已完成 |

## 三、分钟采集 / 统计业务

| 需求 | 结果 | 状态 |
|---|---|---|
| 分钟采集周期统计 | `minute_reports` 持久化、周期聚合（15 分钟默认、按时钟边界对齐）、去重统计 | ✅ 已完成 |
| 分钟分析页面优化 | 周期 1–1440 分钟自由输入、CCO 帧数、上报明细/原文、冻结时间展示、数据状态区分 | ✅ 已完成 |
| 删除配置统计 | 下发与上行应答去重统计、详情查看、APS 原文展示、任务轮次生命周期（启用→删除→再启用） | ✅ 已完成 |

## 四、事件监控（loghooks）

| 需求 | 结果 | 状态 |
|---|---|---|
| FR-1 事件监控 | 配置驱动 + 内容匹配，日志 → 事件时间线/状态机路径/跨来源关联（engine/sequence/rules/对照解析） | ✅ 已实现 |
| 对照解析页 | module_log 页签：事件解析与原始日志双向绑定联动（ADR-6） | ✅ 已完成 |
| 来源卡片化 | 串口/导入文件互斥二选一 + 深色终端美化（ADR-7） | ✅ 已完成 |
| 接入 concentrator_10376 | 第三来源（模拟集中器 13762 帧解析）接入 | ✅ 已完成 |

## 五、模拟集中器（sim_concentrator）

| 需求 | 结果 | 状态 |
|---|---|---|
| FR-2 模拟集中器 | 13762 帧激励与应答、验证任务逐步判定、REST/CLI 闭环（runner/api/cli，统一 adapter_10376） | ✅ 已实现 |
| 前端可视化 | module_log 第三页签「模拟集中器」，挂载 simcon 子应用（ADR-10/13） | ✅ 已完成 |

## 六、桌面交付 / 启动

| 需求 | 结果 | 状态 |
|---|---|---|
| 桌面 exe 打包 | listener / module_log 打包为本地桌面 exe（pywebview 内嵌窗口），保留网页模式（ADR-2/3/8） | ✅ 已完成 |
| 一键启动 | 根目录 `启动工具.bat` 统一菜单拉起（GBK + CRLF 编码，ADR-9/11） | ✅ 已完成 |
| 免安装交付 | PyInstaller onedir 打包、frozen 路径分叉、冒烟脚本、Git LFS 产物入库 | ✅ 已完成 |
| E-SafeNet 环境 | 启动脚本改为启动已构建 exe，加密环境下源码直跑失败已规避（ADR-11） | ✅ 已完成 |

## 七、仓库 / 工程化

| 需求 | 结果 | 状态 |
|---|---|---|
| 目录两阶段迁移 | `apps/` + `libs/` 分层 + docs/tools/data 归组，sys.path 注入、import 名不变（ADR-14） | ✅ 已完成 |
| 记忆后端切火山方舟 | OpenViking embedding/VLM 切换到 doubao-embedding-vision + doubao-seed-code（ADR-12） | ✅ 已完成 |

---

## 附：需求变更追溯（关键节点）

| 时间 | 需求变更/澄清 | 交付结果 |
|---|---|---|
| 2026-08-02 | 新增分钟采集报文分析 | E2/E3/E4 识别、富化、持久化、周期统计 API 与页面 |
| 2026-08-02 | 修正并发抄读格式判定与展示 | 格式切帧、启动位判定、抄表帧富化、DLL 协议字段修正、前端应用层展开 |
| 2026-08-02 | 日志输入升级原生选择体验 | Windows 原生文件选择、上次路径、中文路径、异常/超时加固 |
| 2026-08-02 | 分钟分析页面贴合业务口径 | 周期统计与数据状态展示优化、旧服务自动重启兼容 |
| 2026-08-03~04 | 交付方式扩展为免安装 | onedir 打包、运行时路径适配、构建与冒烟验证、Git LFS 入库 |
| 2026-08-04 | 分钟分析「无数据」口径修正 | 统计口径修复并重建 exe 交付物 |
| 2026-08-04 | 按 NID 缩小排查范围 | 摘要展示与筛选、全局共享、通配符安全处理 |
| 2026-08-04 | 删除配置统计与溯源 | 下发/上行应答去重统计、详情页、APS 原文 |
| 2026-08-07 | 串口实时采集 | 裸 7E 帧流切帧、落盘 + 实时入库、数据源互斥 |
| 2026-08-14 | 日志缓冲/显示框环形裁剪 | 后端 5000 行 + 前端 3000 行，防整夜 OOM |
