# 代办事务

> **文档用途**：记录项目待办需求与任务，跟踪实施进度。
>
> **创建时间**：2026-08-06
> **维护方式**：新增需求追加到末尾，完成的条目改为 ✅ 并标注完成日期

---

## 待办列表

### T-001：一键打包发布改造

- **状态**：📋 待实施
- **创建日期**：2026-08-06
- **方案文档**：[docs/一键打包发布方案.md](./一键打包发布方案.md)
- **需求描述**：

  新建根目录 `一键打包-发布.bat` 脚本，实现一键打包到 `发布/` 文件夹。打包产物拷贝到其他 Windows 10/11 电脑即可直接使用，无需安装 Python 或任何依赖。**发布目录取代现有 `dist/` 目录**。

- **验收标准**：

  1. 双击 `一键打包-发布.bat`，自动完成安装 PyInstaller → 打包 → 冒烟测试全流程，退出码 0；
  2. 产物在 `发布/侦听台/` 目录下，包含 `侦听台.exe` 和 `_internal/` 子目录；
  3. 冒烟测试通过：`/api/version` 返回 `GW_SMAnalysis`，首页可访问，索引+分页正常，`runtime/` 落在 exe 同目录；
  4. 拷贝 `发布/侦听台/` 到其他 Windows 10/11 电脑，双击 `侦听台.exe` 可正常启动服务并打开浏览器；
  5. `dist/` 已移除，`发布/` 的 Git LFS 跟踪和 .gitignore 忽略规则正确生效。

- **涉及改动**：

  | 文件 | 操作 |
  |------|------|
  | `一键打包-发布.bat` | 新建 |
  | `.gitattributes` | LFS 规则 `dist/**` → `发布/**` |
  | `.gitignore` | 忽略规则 `dist/` → `发布/` |
  | `tools/scripts/smoke_test_packaged.py` | 默认 exe 路径改 `发布/侦听台/` |
  | `tools/packaging/hplc_parser.spec` | 文档注释更新 |
  | `tools/packaging/build_exe.bat` | 标注废弃引导 |
  | `dist/` | `git rm -r` 移除 |

- **优先级**：P0
- **预估工时**：1-2 小时

---

## 已完成列表

*（暂无）*
