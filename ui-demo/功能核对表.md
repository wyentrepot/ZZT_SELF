# 94 项功能逐条核对表（Demo v2 对照基线）

> 基线：`ui-demo/功能清单.md`（9 页 + 外壳 / 94 项）
> 被核对对象：`ui-demo/workbench-ui-demo.html`（单文件 Demo v2）
> 核对日期：2026-08-30
> 核对方式：① 静态校验 `_check.js`（id 引用零悬空）② 运行时冒烟 `_smoke.js`（112 断言全通过）③ 人工逐条对位
> 图例：✅ 已实现并可指认 ｜ ⚠️ 已实现但与生产文案/形态略有差异 ｜ ❌ 缺失

**结论：94 / 94 覆盖（✅ 93 · ⚠️ 1）**

---

## 0. 外壳 · Shell（9 项）

| # | 功能 | Demo 落点（元素 id / 区块） | 状态 |
|---|---|---|---|
| 0.1 | 品牌区 | 顶栏 `#app` 左：LOGO +「AI 闭环研发验证工作台」+ 副标题 | ✅ |
| 0.2 | 主题切换 | 顶栏 `#themes` 4 圆点：🌊深蓝科技 / 🌿暗色翡翠 / ✦炭黑灰金 / 🔮靛蓝玻璃 | ✅ |
| 0.3 | 侧栏折叠 | `#btnSide`（Ctrl+B 同步），折叠态存 localStorage | ✅ |
| 0.4 | 状态栏 | `#wbStatus` 顶栏副标题：`模块日志 ✓ / 侦听台 ✓` | ✅ |
| 0.5 | 分组导航 | `#nav` 渲染 4 组（验证 / 设备 / 辅助 / 维护）共 9 页 | ✅ |
| 0.6 | iframe 保活 | 单文件 Demo 无 iframe，改为「设计说明」抽屉 `#btnNotes` → `#notesBody` 中**明文说明**该机制 | ⚠️ |
| 0.7 | hash 路由 | `go()` 写入 `#pageId`，支持深链 / 刷新 / 前进后退（`hashchange`） | ✅ |
| 0.8 | 主题广播 | `broadcastTheme()` 向 `window.frames` postMessage `{type:'wb-theme-change'}`；`#btnNotes` 说明 | ✅ |
| 0.9 | 窄屏抽屉 | `@media (max-width:860px)`：`#drawer` + `#scrim`，Esc 关闭 / `#btnCloseDrawer`，焦点回归 `lastFocus` | ✅ |

## 1. 验证工作台 `p-workbench`（9 项）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 1.1 | 场景选择与 Run 参数 | `#scnSel`（5 场景）、`#fwVer`、`#fwCommit`、`#logDir` | ✅ |
| 1.2 | 跳过开关 | `#skipFlash` 跳过烧录 / `#skipStim` 跳过激励 / `#skipFeed` 跳过反馈 | ✅ |
| 1.3 | 执行控制 | `#btnRun` 执行、`#btnRefreshScn` 刷新场景、`#btnCancelRun` 取消（运行中才显） | ✅ |
| 1.4 | 场景描述 | `#scnDesc` 显示 `expected_flow`，negate 项带 🚫 前缀 | ✅ |
| 1.5 | Run 结果 | `#wbBadge`（run_id + 状态徽章）+ `#runResult` steps 列表 | ✅ |
| 1.6 | 状态轮询 | `tickRun()` 800ms 轮询至终态；五终态 `passed/failed/cancelled/error/inconclusive` | ✅ |
| 1.7 | 取消流程 | `cancelRun()` 走 CANCELLING → CANCELLED 两段，前端继续轮询 | ✅ |
| 1.8 | 证据下钻 | `#evidenceBox` + `#evTotal`，按 source 分组（`$SRC_LABEL` 映射来源名） | ✅ |
| 1.9 | 历史 Runs | `#histBody`，Run 结束后 `pushHist()` 自动刷新 | ✅ |

## 2. 串口配置 `p-serial-profile`（6 项）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 2.1 | 四槽 Profile | `#slotGrid` 渲染 4 张 `.slot`（SLOT 1-4），标题「设备组：四槽运行 Profile 保存与一键应用」 | ✅ |
| 2.2 | 工具栏 | `#btnSave` 保存 / `#btnApply` 一键应用 / `#btnSpRefresh` 刷新状态 / `#saveHint` 提示 | ✅ |
| 2.3 | 错误汇总条 | `#spError`（`.banner`，`tabindex="0"` 可聚焦），校验失败时 `.show` 并 `focus()` | ✅ |
| 2.4 | 槽卡片 | 「SLOT n」等宽 ID + `.chk input[data-f="on"]` 启用开关 + 串口/波特率/校验位/数据位/停止位/模块类型/备注 + `.slot-err[data-err]` 字段级错误 | ✅ |
| 2.5 | 槽状态区 | `.slot-st`：已禁用 / 缺串口 / 就绪 · 端口 / 波特率（`slotState()`） | ✅ |
| 2.6 | 结果反馈 | `.slot-res[data-res]` 每槽独立结果（`.ok` 绿 / 默认红） | ✅ |

## 3. 模块日志 `p-module`（21 项）

### 3A 实时日志（3.1-3.8）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 3.1 | 动态会话页 | `#msSessTabs` 会话页签（running 发光点 / error 红点）、`#msAddSess` 新增、`#msCloseSess` 关闭、`#msSessMsg` 会话消息 | ✅ |
| 3.2 | 会话头 | `#msBadge`（CCO/STA 徽章）、`#msTitle` 页面名、`#msSessStatus` 状态 | ✅ |
| 3.3 | 串口参数 | `#msModule` / `#msPort` / `#msRefreshPorts` / `#msBaud`（9600~921600）/ `#msParity`（N/E/O/M/S）/ `#msByte`（5-8）/ `#msStop`（1/2）/ `#msToggle` 启动 | ✅ |
| 3.4 | 烧录参数 | `#msBin` 固件 .bin 路径 + `#msPick` 选择…（文件对话框）/ `#msSlot` image 0/1 / `#msNoReboot` 烧录后不重启 / `#msFlash` 开始烧录 | ✅ |
| 3.5 | 烧录进度 | `#msProgWrap` + `#msProgBar` + `#msProgTxt` | ✅ |
| 3.6 | 日志元信息 | `#msLogPath` 日志路径、`#msAutoscroll` 自动滚动、`#msClear` 清屏 | ✅ |
| 3.7 | 日志框 | `#msLog` 四类着色 `rx / tx / ev / err` | ✅ |
| 3.8 | **串口发送框**（用户点名） | `#msSender` 容器：`#msSendText` textarea + `#msSendBtn` 发送 + `#msSendNl` 换行 + `#msAppendNl` 自动补换行 + `#msSendClear` 清空 + `#msSenderHide`/`#msSenderShowBar`/`#msSenderShow` 隐藏显示；**Enter 发送 / Shift+Enter 换行**；发送目标 `#msSendTarget` | ✅ |

### 3B 对照解析（3.9-3.16）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 3.9 | 来源二选一 | `.srccard[data-src="realtime"]` 实时串口 🔌 / `[data-src="file"]` 导入日志 📂，互斥 + ✓ 选中 | ✅ |
| 3.10 | 模块分段 | `#cmpModule` CCO / STA 切换 | ✅ |
| 3.11 | 文件配置 | `#cmpFile` 路径 + `#cmpPickFile` 打开文件 + `#cmpPickDir` 打开目录 + `#cmpRunFile` 解析日志 | ✅ |
| 3.12 | 串口配置 | `#cmpSession` 实时页面下拉 + `#cmpRtChip` 串口状态 + `#cmpRunRealtime` 开始解析 | ✅ |
| 3.13 | 统计条 | `#cmpStatEvents` 事件 / `#cmpStatLines` 日志行 / `#cmpStatFiles` 来源 / `#cmpStatDrift` **行号漂移** | ✅ |
| 3.14 | 左：事件解析 | `#cmpEvList` 事件卡（标签/时间/消息/类型/规则）+ info/warn/error 图例 | ✅ |
| 3.15 | 右：原始日志 | `#cmpLogList` 虚拟滚动（spacer + window，`ROW_H=18`） | ✅ |
| 3.16 | 双向绑定 | `cmpSelectEv()` / `cmpSelectLine()` 两侧高亮联动；无对应事件的行点击后清除选中 | ✅ |

### 3C 模拟集中器内嵌版（3.17-3.20）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 3.17 | 串口控制 | `#msSimPort` / `#msSimBaud` / `#msSimParity` / `#msSimByte` / `#msSimStop` + `#msSimOpen` 打开 / `#msSimClose` 关闭 / `#msSimRefresh` 刷新端口 / `#msSimStatus` 状态 | ✅ |
| 3.18 | 应答规则 | `#msRuleList` 内置 + 覆盖规则（6 条，可点击开关）+ `#msRuleCnt` 计数 | ✅ |
| 3.19 | 验证任务 | `#msTaskInput` JSON 编辑器（含示例 `verify.demo`）+ `#msRunTask` 执行 + `#msTaskResult` 结构化结果 | ✅ |
| 3.20 | 接口提示 | 页内标注 `REST: /api/simcon/verify` 与 CLI 等价命令 | ✅ |

### 3D 文件选择对话框（3.21）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 3.21 | 文件浏览器 | `openFileDialog()`：上一级 / 根路径快捷（`#FS_ROOTS`）/ 目录列表 / 选择此文件 / 取消；由 `#msPick` 触发 | ✅ |

## 4. 侦听台 `p-listener`（29 项）

### 4.0 全局区（4.1-4.3）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 4.1 | DLL 状态 | `#dllStatus` 芯片「已连接 GwHPLCAnalysis.dll」 | ⚠️ 文案为已连接态（生产含「正在连接」过程态） |
| 4.2 | **全局筛选条**（用户点名） | `#nidFilter` **全局 NID** + `#startTime` / `#endTime` + `#btnApplyFilter` 应用筛选 + `#filterSummary` 当前条件摘要 + `#filterChips` 筛选 chips | ✅ |
| 4.3 | 视图页签 | `#lsnTabs` 5 个：新版帧浏览 / 帧浏览（经典）/ 分钟采集分析 / 任务配置统计 / 网络承载评估 | ✅ |

### 4A 新版帧浏览（4.4-4.6）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 4.4 | 列1 筛选 | `#proQuery` 摘要关键字 + `#proNid` **NID 筛选** + 数据入口说明 + `#proReload` 刷新索引 + `#proToTrace` 打开报文追踪跳转 | ✅ |
| 4.5 | 列2 帧索引 | `#proList` 帧列表 + `#proPrev`/`#proNext`/`#proPageInfo`/`#proPage` 分页 | ✅ |
| 4.6 | 列3 深度解析 | `#proDetail` 单帧详情；**`feature_hint` 可一键送报文追踪** | ✅ |

### 4B 帧浏览（经典）（4.7-4.14）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 4.7 | 01/数据源 折叠面板 | `#loaderToggle` 折叠 + `#loaderMini` 迷你进度条 + `#loaderSummary` 摘要「未加载日志」 | ✅ |
| 4.8 | 单帧调试 | `#singleToggle` 面板：`#frameInput` 十六进制 textarea + `#btnParseOne` 解析这一帧 + `#singleOutput` 输出 pre | ✅ |
| 4.9 | 数据源二选一 | `#dataSrcSeg`：日志文件分析 / 串口实时监听 | ✅ |
| 4.10 | 日志模式 | `#logPath` 路径 + `#btnPickLog` 选择日志文件 + `#btnSample` 开发样本 + `#btnLoadLog` 建立解析索引；`#progressBar`/`#progressValue`/`#bytesRead`/`#frameCount`/`#errorCount` 四项统计 | ✅ |
| 4.11 | 串口模式 | `#serialPort` + `#serialPortRefresh` + `#serialBaud` + `#serialByte` + `#serialParity` + `#serialStop` + `#serialStart` 启动采集 / `#serialStop` 停止 / `#serialRefresh` 刷新帧列表 / `#serialState` 状态 | ✅ |
| 4.12 | 02/帧索引 | `#frameFilter` 筛选框 + `#pageSize` 每页帧数(1~500) + `#frameRows` 表格（#/日志时间/帧类型/**NID**/源→目的/通道/长度/状态） | ✅ |
| 4.13 | 分页 | `#prevPage` / `#pageNumber` / `#pageJump`+`#btnJump` 跳转 / `#nextPage` / `#cacheHint` 缓存提示 | ✅ |
| 4.14 | 03/深度解析 | `#detailBadges` 徽章组 + 详情页签（`.tab[data-dt="base"]` 基础解析 / `.tab[data-dt="app"]` **应用层展开** → `#appExpand`）+ `#parseStages` 分层 + `#detailJson` 完整 JSON + `#detailRaw` 原始十六进制 + `#copyDetail` 复制 | ✅ |

### 4C 分钟采集分析（4.15-4.18）← 用户点名

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 4.15 | 查询条件 | `#minuteSrc` 来源（配置推导/手工输入）+ `#minuteTask`+`#minuteTaskList` 任务号（datalist 可选可输）+ `#minutePeriod` 周期(1~1440) + `#minuteTei` **CCO TEI** + `#minuteQuery` 查询 | ✅ |
| 4.16 | 状态提示 | `#minuteErr` **错误横幅**（`.banner` `tabindex="0"`，入参非法时展开并聚焦）+ `#minuteNidHint` **NID 提示** + `#minuteScope` 当前筛选范围文案 | ✅ |
| 4.17 | 任务周期统计表 | `#minuteTable` 周期列表（行 `data-mi` 可点） | ✅ |
| 4.18 | 周期上报详情 | `#minuteDetail` 右侧栏：点周期后列出该周期的分钟采集上报 | ✅ |

### 4D 任务配置统计（4.19-4.23）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 4.19 | 查询条件 | `#taskTei` CCO TEI + `#taskSel` 任务号下拉 + `#taskRefresh` 刷新任务号 + `#taskQuery` 查询统计 | ✅ |
| 4.20 | 统计数字 | 6 项：`#taskSent` 下发 STA / `#taskOk` 应答成功 / `#taskFail` 应答失败 / `#taskNoResp` 未应答 / `#taskPending` 等待应答 / `#taskUnissued` 未下发上报 | ✅ |
| 4.21 | 状态图例 | 成功 / 失败·未应答 / 等待处理（页内图例行） | ✅ |
| 4.22 | STA 汇总表 | `#taskStaTable` + `#taskSortMac` **STA MAC 可点击排序(↑↓)**；方向 / 配置类型 / 最近下发时间 / 应答时间 / 状态 / 报文序号 | ✅ |
| 4.23 | 轮次分析 | `#cycleSel` 下拉选轮次 + `#cycleAnalysis` 显示生命周期、删除状态和异常 | ✅ |

### 4E 网络承载评估（4.24-4.28）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 4.24 | 操作 | `#netRefresh` 刷新快照 + `#netAssess` 详细评估 | ✅ |
| 4.25 | 快照四指标 | `#netBeacon` 当前实测信标周期 / `#netRate` 最近周期成功率 / `#netRating` 最近周期评级 / `#netHealth` 整体健康度 | ✅ |
| 4.26 | 趋势图 | `#netChart` canvas，`drawNetChart()` 绘制「周期成功率趋势」（取 CSS 变量着色，主题切换同步） | ✅ |
| 4.27 | 网络指标块 | `#netMetrics` 动态指标网格 | ✅ |
| 4.28 | 周期明细表 | `#netCycleRows`：周期起止 / 实测周期值 / 帧数 / 关联数 / 代理变更 / 帧率 / 评级 + **隐藏 NID 列**（`<th style="display:none">NID</th>`） | ✅ |

### 4F 文件选择对话框（4.29）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 4.29 | 文件浏览器 | `openFileDialog({pickDir:true})`：上一级 + **系统文件管理器**（打开 Windows 资源管理器）+ 路径 + 根快捷 + 列表 + 选择/取消；由 `#btnPickLog`、`#cmpPickDir` 触发 | ✅ |

## 5. 模拟集中器 `p-simcon`（7 项）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 5.1 | 串口控制 | `#simPort` + `#simBaud`(2400~115200) + `#simParity`(E/N/O) + **`#simProfile` Profile(anhui/test)** + `#simOpen` / `#simClose` / `#simStatusChip` `#simStatusTxt` 状态芯片 + `#simRefreshPort` + `#simProfileTag` 标签 + `#simHelp` | ✅ |
| 5.2 | 列1 AFN | `#afnList` 应用功能码列表（15 项）+ `#afnCount` 计数 | ✅ |
| 5.3 | 列2 Fn | `#fnList` 命令列表（依赖 AFN 选择）+ `#fnCount` 计数 | ✅ |
| 5.4 | 列3 参数与下发 | `#simParamBox` 语义化参数表单（send 只写业务参数，构帧自动完成，`PARAM_TPL`） | ✅ |
| 5.5 | 下发区 | `#simPreview` 刷新预览 + `#simFrameMeta` 帧元数据 + `#simSend` 下发（串口未打开时 disabled） | ✅ |
| 5.6 | 内置应答条 | 底部 `.respStrip` → `#simRespTxt` 内置应答状态 | ✅ |
| 5.7 | 收发记录 | `#simlogToggle` 可折叠；`#simTxCnt` / `#simRxCnt` 收发计数；`#simLogSeg` 分段筛选（全部/发送/接收）；`#simLogList` 帧日志 + `#simLogClear` | ✅ |

## 6. 报文追踪 `p-trace`（3 项）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 6.1 | 追踪任务 | `#traceModeSeg` 模式分段（**回放 / Live**）+ `#traceNew` 新建 + `#traceReload` 刷新 + `#traceCount` 计数 + `#traceList` + `#traceModeTag` + `#traceFromListener`（侦听台跳转入口） | ✅ |
| 6.2 | 特征 · 新建追踪 | 中列 `#traceEditTag`：`#trName` / `#trField` / `#trOp` / `#trValue` / `#trWindow` / `#trLimit` / `#trStrict` + `#trSave` / `#trReset` / `#trDelete` + `#trHint` | ✅ |
| 6.3 | 三段证据链 | 右列 `#traceEvid` + `#traceEvSub` 三段式证据展示 | ✅ |

## 7. 协议字典 `p-dict`（3 项）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 7.1 | 三列 | `#dictList` 字典列表 / `#dictItems` 条目 / `#dictDetail`+`#dictDetailKey` 条目详情 | ✅ |
| 7.2 | 搜索 | `#dictSearch` 按名称 / 键 / 描述过滤（`DICT_DATA` 4 本字典 20 条） | ✅ |
| 7.3 | 计数 | `#dictListCount` 字典数 + `#dictItemCount` 条目数 + `#dictTotalTag` | ✅ |

## 8. 场景脚本 `p-scenario`（5 项）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 8.1 | 三列 | `#scList` 场景 / `#scFlow` 步骤编排 / `#scStep` 步骤详情 | ✅ |
| 8.2 | 场景卡 | 名称 + 等宽 ID + 元信息 chips（5 个场景，`#scListCount` 计数） | ✅ |
| 8.3 | 步骤编排 | `#scFlowSub` + `buildFlow()` 渲染 `expected_flow` 流程时间线（序号/名称/事件/毫秒） | ✅ |
| 8.4 | 步骤详情 | `#scStep`：语义化参数（ADR-5）+ JSON 原文 | ✅ |
| 8.5 | 跳转 | 顶栏 `#scnGoWorkbench`「去验证工作台执行」（`#workbench`） | ✅ |

## 9. 工作台状态 `p-maintenance`（2 项）

| # | 功能 | Demo 落点 | 状态 |
|---|---|---|---|
| 9.1 | 平台信息 | `#mntReload` + `#mntInfo`：`#mApp` 应用名 / `#mVer` 版本 / `#mMod` 模块日志已挂载✓ / `#mLsn` 侦听台已挂载✓ | ✅ |
| 9.2 | 主题说明 | `#mThemeName` 当前主题名 + `#mntThemes` + `#mThemeHint`；监听 `wb-theme-change` 事件同步（`syncMntThemes()`） | ✅ |

---

## 用户点名三处专项复核

| 用户点名项 | 清单编号 | Demo 落点 | 冒烟断言 | 结论 |
|---|---|---|---|---|
| 模块日志 · 串口发送数据框 | 3.8 | `#msSender` … `#msSenderShow` | 「串口发送框发出内容」「发送后清空输入框」「收到模拟应答」 | ✅ |
| 侦听台 · 分钟采集分析页面 | 4.15-4.18 | `#lv-minute` 整个视图 | 「分钟采集周期统计有行」「周期上报详情渲染」「NID 提示因锁定显示」 | ✅ |
| 侦听台 · NID 锁定 | 4.2 / 4.4 / 4.12 | `#nidFilter` 全局条 + `#proNid` 列1 + 帧索引 NID 列 | 「全局 NID 筛选生效」「筛选 chips 渲染」 | ✅ |

## v1 → v2 缺失项补齐对照

清单末尾登记的 16 处 v1 缺失，**全部补齐**：

| v1 缺失项 | v2 落点 |
|---|---|
| 模块日志 · 串口发送框 | 3.8 `#msSender` |
| 模块日志 · 对照解析页签 | 3.9-3.16 `#tp-compare` 整套 |
| 模块日志 · 动态会话页 | 3.1 `#msSessTabs` + `#msAddSess` |
| 侦听台 · 分钟采集分析 | 4.15-4.18 `#lv-minute` |
| 侦听台 · 任务配置统计 | 4.19-4.23 `#lv-task` |
| 侦听台 · 网络承载评估 | 4.24-4.28 `#lv-net`（含 `#netChart` canvas） |
| 侦听台 · 全局 NID 筛选 | 4.2 `#nidFilter` + `#filterChips` |
| 侦听台 · 新版帧浏览 | 4.4-4.6 `#lv-pro` |
| 场景脚本页 | 8.1-8.5 `#p-scenario`（v1 只做 8 页） |
| 串口配置真实形态 | 2.1-2.6 改为**四槽 Profile 保存与一键应用** |
| 验证工作台跳过项 | 1.2 改为跳过烧录 / 激励 / 反馈 |
| 验证工作台固件信息 | 1.1 `#fwVer` / `#fwCommit` / `#logDir` |
| 取消 Run | 1.3/1.7 `#btnCancelRun` + CANCELLING→CANCELLED |
| 证据下钻 | 1.8 `#evidenceBox` |
| 主题切换 | 0.2 `#themes` 4 套 + `broadcastTheme()` |
| 页面命名 | 导航为「协议字典」「工作台状态」（v1 误作「报文字典」「维护」） |

## 自动化校验结果

| 校验 | 工具 | 结果 |
|---|---|---|
| JS 语法 | `_check.js` | PASS（约 104 KB 内联 script，`new Function` 通过） |
| 悬空 id 引用 | `_check.js` | PASS（258 个 `$('#id')` 引用零悬空） |
| 导航 ↔ 页面映射 | `_check.js` | PASS（9 导航 ↔ 9 `<section id="p-*">` 全映射） |
| 标签开闭平衡 | `_check.js` | PASS |
| 4 套主题 CSS 齐备 | `_check.js` | PASS（`theme-deepblue/emerald/charcoal/indigo`） |
| 用户点名三处存在 | `_check.js` | PASS |
| 运行时冒烟（9 页） | `_smoke.js`（jsdom） | PASS：**114 断言通过 / 0 失败 / 0 运行时错误** |

## 校验中发现并已修复的问题（T115）

| # | 问题 | 性质 | 修复 |
|---|---|---|---|
| 1 | 串口配置 `change` 后整体 `renderSlots()` 重绘 → 丢焦点、清空每槽结果、旧节点事件不再冒泡 | **真实缺陷** | 新增 `updateSlotCard(i)`，只更新该槽状态行，改为局部刷新 |
| 2 | 分钟采集缺「错误横幅」（清单 4.16） | 功能缺失 | 新增 `#minuteErr`（`.banner` `tabindex="0"`）+ `minuteQuery()` 入参校验（TEI 非空、周期 1~1440） |
| 3 | `parseInt(v,10) \|\| 15` 把周期 `0` 吞成 `15`，非法值绕过校验 | **真实缺陷** | 改为 `isNaN(pv) ? 15 : pv` |
| 4 | 冒烟脚本 `$$('#detailBase').parentNode`（`$$` 返回数组） | 脚本 bug | 改用 `.tab[data-dt="app"]` |
| 5 | 冒烟脚本对照解析「点日志行」点的是首行（无对应事件，按设计清高亮） | 断言写错 | 改为点事件所在行 `data-ln`，并补一条「点无事件行清除选中」的正向断言 |
| 6 | 静态校验误报 `<tr> 28 开 / 29 闭` | 校验脚本 bug | 开标签正则改为 `<tag(?=[\s>'"`])`，覆盖 JS 拼接写法 |
