# 汇报.md —— 13-设计契约偏差核查 修复执行汇报

> 汇报日期：2026-08-18
> 执行基线：`master` `013d4b9`（docs: reconcile design contracts with implementation）
> 修复依据：`docs/_archive/2026-08-18/13-设计契约偏差核查.md`（4 项真实缺口）
> 修复范围：D-01~D-04 全部代码修复 + 验收测试 + 08 硬件手册核查

---

## 1. 任务总览

按用户指示执行了 `git pull`，从最后一次提交记录定位到 `13-设计契约偏差核查.md`，确认其中 **4 项真实未闭环缺口（P0×2 + P1×2）**，并全部完成代码修复与测试验证。同时按 `08-真实硬件验证手册.md` 核查了物理连接可自行验证的部分，输出本汇报。

**已完成（可自行验证）**：
- ✅ D-01 RunStatus 契约统一（P0）
- ✅ D-02 inconclusive 三源缺失判定（P0）
- ✅ D-03 Artifact 审计链（P1，方案 A 完整）
- ✅ D-04 统一错误响应（P1）
- ✅ 23 项新增验收测试 + 全量相关测试跑通
- ✅ 08 手册硬件自检（无硬件部分）+ 烧录 dry-run

**需人工配合（未执行，见 §6）**：
- ⏸ 真机烧录（写固件，不可回退）
- ⏸ COM24 交互验证（写设备）
- ⏸ COM4 侦听口与 COM24 交互口的 tty 归属确认（手册标注的推断项）

---

## 2. Pull 与文档定位

- 执行 `git pull`：`edac2c3..013d4b9` fast-forward，39 文件更新。
- 最后一次提交 `013d4b9 docs: reconcile design contracts with implementation` 引入了 `13-设计契约偏差核查.md`（随 05~12 号文档一起移入 `docs/_archive/2026-08-18/`）。
- 文档核心结论：**4 项真实未闭环**（D-01~D-04），6 项设计口径澄清，2 项有意演进，8 项已确认一致。

---

## 3. 四项缺口修复明细

### D-01（P0）workbench RunStatus 与规范枚举不一致 —— 已修复

- **问题**：`apps/workbench/orchestration/models.py` 声明 `Literal["pending","running","passed","failed","aborted"]`，而规范模型 `libs/test_automation.models.RunStatus` 含 `created/queued/running/cancelling/cancelled/passed/failed/inconclusive/error`；runner 实际写入 `cancelling/cancelled`，`aborted` 无人用。
- **修复**：
  - workbench `RunStatus` 直接复用规范枚举（`from test_automation.models import RunStatus`），单一事实来源。
  - `Run.status` 初态改为 `RunStatus.CREATED`（规范无 `pending`）。
  - runner 所有状态赋值改枚举对象（`RunStatus.PASSED/CANCELLED/FAILED/INCONCLUSIVE/RUNNING`）。
  - `aborted` 被 pydantic 拒绝（消除死枚举值）。
- **验收测试**：`TestRunStatusUnified`（复用枚举/初态/cancelling/cancelled/aborted 拒绝/inconclusive 终态）。

### D-02（P0）Run 级结论缺少 inconclusive —— 已修复

- **问题**：`Report.verdict` 只有 pass/fail，必要证据缺失时无法表达 inconclusive。
- **修复**：
  - `Report.verdict` 扩展为 `pass|fail|inconclusive`；`Assertion.result` 三态。
  - runner 新增 `_verdict_inconclusive_reasons` 三源缺失判定：
    - monitor 未跳过但 0 事件 → 缺失
    - stimulus 未跳过但无执行结果 → 缺失
    - 显式期望 listener（传 `listener_index`）但无帧 → 缺失
  - 判定优先级：**simcon 明确失败 → fail**（真实失败优先）＞ **证据缺失 → inconclusive** ＞ **compare missing → fail** ＞ pass。
  - 缺失原因登记为 `source.missing` 的 inconclusive 断言（证据链可追溯）。
  - 前端 `workbench.html` / `tokens.css` 增加 inconclusive 终态展示（badge「不确定」）。
- **兼容性**：用户主动 `skip_*` 的源不判缺失（保持现有降级运行行为），现有 62 个测试全部保持通过。
- **验收测试**：`TestInconclusive`（monitor 缺失 / stimulus 缺失 / listener 无帧 / simcon fail 优先）。

### D-03（P1）Artifact 审计链未落地 —— 已修复（方案 A 完整）

- **问题**：规范 `Artifact` 已定义但执行路径未用；`Report.artifacts` 只是日志路径字符串；无下载接口。
- **修复**：
  - 新增 `apps/workbench/orchestration/artifacts.py`：`find_artifact`（逻辑 ID 查 manifest）、`resolve_artifact_path`（路径越界防护：不存在/目录/未登记 → 拒绝）。
  - `Report.artifacts` 结构化 `ArtifactInfo`（逻辑 ID `run-xxx-art-N` / type / name / sha256 / path / size / created_at）。
  - runner `_build_artifacts` 对 monitor 产物逐文件计算 SHA-256，生成 manifest。
  - `api.py` 新增 `GET /api/run/{run_id}/artifacts`（列 manifest）与 `GET /api/run/{run_id}/artifacts/{artifact_id}`（下载，越界 → 404）。
- **验收测试**：`TestArtifactManifest`（SHA-256 正确/缺失不登记/结构化）、`TestArtifactResolve`（越界/目录/无路径拒绝）、`TestArtifactDownloadApi`（下载 200 内容一致/未知 404）。

### D-04（P1）错误响应无统一结构 —— 已修复

- **问题**：API 用 FastAPI 默认 `HTTPException(detail=...)`，无 `code/message/details/request_id`。
- **修复**：
  - 新增 `apps/workbench/errors.py`：`api_error_body` 生成 `{code, message, details, request_id, detail}`（**保留 detail 兼容前端**）。
  - `register_error_handlers` 注册 HTTPException / RequestValidationError / 未捕获 Exception 处理器。
  - 500 响应不泄漏内部路径/句柄。
  - `app.py` 注册。
- **验收测试**：`TestUnifiedErrorResponse`（404/409/422 统一结构 + 字段级 details + detail 兼容）。

---

## 4. 测试验证结果

| 范围 | 结果 |
|---|---|
| 新增验收测试 `test_contract_fixes.py` | **23 passed**（D-01~D-04） |
| `apps/workbench/` 全量 | **85 passed**（3 连跑稳定） |
| `libs/test_automation/ + apps/workbench/` | **143 passed** |
| `libs/ + apps/workbench/`（排除 DLL 依赖） | **386 passed, 66 skipped** |

**已知失败项（非本改动引入）**：
- `libs/shared/test_dll_python_meter_consistency.py` 5 项失败，根因缺失 `GwHPLCAnalysis.dll`（.NET 解析库）。已用 `git stash` 验证：**基线 HEAD 同样失败**，与 13 号文档 §9 记录的已知间歇性/依赖 DLL 问题一致，不在本次修复范围。

---

## 5. 08 硬件手册核查（2026-08-18）

### 5.1 可自行验证部分 —— 全部通过 ✅

| 核查项 | 结果 |
|---|---|
| 串口映射（§1） | `/dev/ttyUSB0`、`/dev/ttyUSB1`、`/dev/ttyUSB2` 三个串口均存在、未被占用；经 `vhci_hcd`（usbip）映射。`/dev/ttyACM0` 未出现（手册提到 CH343，当前无） |
| 无硬件自检（§2） | `libs/sim_concentrator/` **53 passed**（手册期望 53）；`flash_module.py --selftest` **Self-test PASS**（CRC 0x31C3） |
| 固件与 slot（§3） | 固件目录齐全：`flash_cco_AN_HUI_*.bin`（2MB）、`iap_cco_AN_HUI_*.bin`（464KB，手册推荐 IAP）、`upgrade_cco_*.dat` |
| 烧录 dry-run（§4） | **Dry-run PASS**：Port=/dev/ttyUSB2，Firmware=iap 464432B，Baud plan=[9600,115200,9600]，Command=`download 0`（slot 0） |

### 5.2 需人工确认/授权部分 —— 未执行 ⏸

| 项 | 说明 |
|---|---|
| **真机烧录**（§4 步骤 2） | `flash_module.py --port /dev/ttyUSB2 --bin iap_... --slot 0`。写固件**不可完全回退**，需用户授权后执行 |
| **COM24 交互验证**（§5） | `sim_concentrator verify anhui_minute_collect.json --port ... --baud 9600 --json`，9 步写设备操作，需人工配合 |
| **COM4 侦听口归属确认**（§1） | 手册标注 COM4=/dev/ttyUSB0 是**推断**（两个 CP210x 未实测区分）。当前 udev 无 ID_SERIAL（usbip 不传递），需在 Windows 设备管理器按物理端口号人工确认 USB0=COM4、USB1=COM24 |
| **slot 实际值确认**（§3） | 默认 0；若设备当前从 slot 1 启动，`download 1` 需真机确认 |
| **Ln 字节数真机结论**（§5 重点） | 11H-F231 的 Ln=2 字节真机结果需真机验证后回填 |

---

## 6. 结论与后续

1. **四项真实缺口全部修复**，验收测试 23 项 + 相关全量测试通过。
2. 修复未提交 git（保持工作区，供用户检视 diff 后决定提交）；改动清单见 §7。
3. 硬件验证中**可无风险完成的自检/dry-run 已全部通过**，说明烧录参数与固件链路正确。
4. **下一步需人工**：授权真机烧录 + COM24 交互验证 + COM4 归属确认；完成后回填 08 手册 §7 产出证据，并据结果更新 `04-任务安排.md` 任务 2 状态。

---

## 7. 本次改动文件清单

```
修改：
  apps/workbench/orchestration/models.py   # D-01 枚举复用 + D-02 verdict 三态 + D-03 ArtifactInfo
  apps/workbench/orchestration/runner.py    # D-01 枚举赋值 + D-02 缺失判定 + D-03 manifest
  apps/workbench/api.py                     # D-03 下载接口 + D-04 依赖错误结构
  apps/workbench/app.py                     # D-04 注册异常处理器
  apps/workbench/static/workbench.html      # D-02 inconclusive 前端展示
  apps/workbench/static/tokens.css          # D-02 inconclusive badge 样式
新增：
  apps/workbench/errors.py                  # D-04 统一错误响应
  apps/workbench/orchestration/artifacts.py # D-03 Artifact 解析 + 越界防护
  apps/workbench/orchestration/test_contract_fixes.py  # D-01~D-04 验收测试（23 用例）
未提交（untracked，非本任务）：docs/_evidence/
```
