# 高频采集失败分析规则（hf-collect-analysis 参照）

> 来源：2026-08-20 实测日志分析（`_tmp_hf_analyze` 时的结论）。本文件是分析口径
> 的权威参照，SKILL.md 是执行步骤。如分析结论与本文件冲突，以最新日志实测为准并更新本文件。

## 输入日志格式

| 日志 | 编码 | 行格式 | 时间可靠性 |
|---|---|---|---|
| 台体 | GBK | `2026-08-20 16:42:37:272 MTC@admin-PC: "send cmd to cco:'68..'"` | 不可靠（可快/慢几分钟），只作动作序列 |
| CCO | UTF-8（带 BOM） | `[20260820_16:42:37_272]#...` | 基本准；`ApsReadRecord` 滞后到达 30~50s |
| 侦听台 | UTF-8 | `[     1][16:42:37.272]7E FF 02 ...` | **唯一时间基准** |

三份日志时钟不同步时，一律以侦听台为基准。

## 关键帧特征

- **采集帧**（台体 send cmd 中）：`630198900000<12位hex地址>F101`，afn=0x63，地址 6 字节小端。
- **建档案帧**：`68 LL 00 43 ... 11 01 00 <cnt> <addr6B type1B> x cnt`，type=0x03 为 DL/T 698.45。
- **CCO 回读成功**：`recv,ApsReadRecord`。
- **CCO 发送**：`aps tx ... dst ...`。
- **表地址编码**：台体侧正序 12hex（`020000012201`）；CCO/侦听台侧小端 6 字节反转
  （`022201000002`）。分析器自动双向转换匹配。

## 判定口径

1. **失败表候选**：台体中 `send >= 3` 且从未 `ReadMeter Success` 的表。
2. **二次证据**：
   - 只有请求帧/aps tx、无 `ApsReadRecord`/应答数据 → **STA/电表未响应**
   - 大量 retry/队列积压 → **网络拥堵**
3. **时间折算**：CCO `ApsReadRecord` 比实际到达滞后 30~50s；台体时间需对齐侦听台。

## 2026-08-20 样例结论（复现基准）

- 失败表：`010000012201`、`020000012201`（0x22 网关下挂 698 电表）
- 侦听台：012201 自 16:42:01 被反复抄读 7+ 次，持续 ~2.5 分钟无应答，16:44:48 才首次回读
- 台体：`read cycle reach max(3)` 后放弃，早于电表回读约 40+ 秒
- CCO：采集期无 012201 的 `ApsReadRecord`；16:43:55 才向 tei 0022 发送且 tx_cnt 3
- 根因：**入网晚、响应时延大、补抄窗口不足 → 超时判失败；非网络拥堵**（其余 92+ 表秒级成功）

## 自带精简样例

`tools/taiti/高频采集/{台体,CCO,侦听台}/samples/` 三份文件由原始 8MB/4MB/12MB 日志
按规则筛选而来（保留与失败表、时间窗相关的行，编码保持），单测
`tools/taiti/高频采集/test_hf_collect_analyze.py` 依赖它们，可作回归基线。

## 2026-09-02 黑龙江友讯达分钟级采集复盘（新增规则）

- **先判入网门禁，再跑高频采集**：successRate >= 0.98 只能说明达到软件阈值，不能替代测试要求中的“全部入网”。应要求最终为 1.000000，并对比预期档案地址与拓扑地址集合。
- **地址需跨视图转换后关联**：台体采集帧地址 170000011101 对应拓扑打印 MAC 01 11 01 00 00 17。分析“未入网”和“未读表成功”时，必须按字节序反转后再判断是否为同一节点。
- **分开看三项判据**：一个样本可以读表成功率 >=98%，但仍因节点未全入网或总时长超窗口而失败。输出必须分别列出拓扑完成度、唯一读表成功地址集合和总耗时。
- **忙/并发上限不是独立根因**：通过样本也可出现大量忙提示和多次补抄。只有某地址 send >=3、从未 ReadMeter Success，并且拓扑/侦听台/CCO 证据同向时，才可将它作为异常节点深入定位。
- **本次基准**：通过样本为 145/145、拓扑 1.0、45.160931 s；失败样本为 144/145、拓扑 0.993103、缺失 170000011101、72.656554 s。失败的时间差主要集中在第一段的异常节点等待，而非第二段或忙提示总数。

- **双段补发的计数口径**：有 First duration 与 Second duration，且每表均至少发送 2 次时，以 2 次为基线，额外补发为 sum(max(send_count - 2, 0))；不得把第二段正常首发误计为补发。109/111 并发提示与补发没有事务 ID 一一对应，只能按地址发送次数统计，再用 CCO/侦听台确认逐报文因果。

- **补发状态机与超时层级**：收到结果后立即补的是“下一个待发并发槽位”，不是同一地址的即时重发；同一地址在全局 ti_wait_new_report 无新报告约 5 s 后进入下一 cycle 再补抄。cycle 0 为首发，cycle 1～3 对应最多 3 轮补抄；拓扑 wait 60 s、分钟级效率 60 s 与该约 5 s 的无报告等待分别是三层不同时间限制。
